# Checkpoint ممیزی — Label Mod Core

> این فایل حافظهٔ ممیزی است. Context از بین می‌رود؛ این فایل نمی‌رود.
> پس از هر گام معنادار به‌روز می‌شود. بدون راز، بدون PII، بدون توکن.

## مبنا (فاز ۰ — ثبت‌شده)

| قلم | مقدار |
|---|---|
| تاریخ شروع | ۱۴۰۵/۰۶/۲۱ · 2026-09-12 |
| SHA | `709ff21e6252304531f3895af477425189238b33` |
| شاخهٔ کار | `claude/friendly-maxwell-27vhz3` |
| Working Tree در شروع | پاک |
| شاخه‌ها | `main`، `claude/friendly-maxwell-27vhz3` (محلی و remote) |
| Node | v22.22.2 |
| pnpm | 10.33.0 (از `packageManager`) |
| PostgreSQL | 16.13 (کلاستر محلی `16/main`، نه Docker) |
| Docker CLI | 29.3.1 — **دیمن اجرا نمی‌شود** (`/var/run/docker.sock` وجود ندارد) |
| PHP | 8.4.19 CLI |
| WordPress / wp-cli | نصب نیست |
| Chromium | `/opt/pw-browsers/chromium` موجود |
| منطقهٔ زمانی سرور | `Etc/UTC` — همان شرطی که کلاس باگ `::date` را آشکار می‌کند |
| هوک‌ها | نصب **نبودند**؛ در شروع ممیزی نصب نشدند (کار روی شاخهٔ مستقل) |

`HEAD` دقیقاً همان مبنای مرجع پرامپت (`709ff21`) است.

## خط پایهٔ تست (اجرای واقعی، نه از حافظه)

| مجموعه | فرمان | کد خروج | نتیجهٔ واقعی | عدد `CLAUDE.md` |
|---|---|---|---|---|
| مهاجرت ۰۰۱–۰۴۸ | `ops/db.sh migrate` | ۰ | ۴۸ فایل اجرا شد | ۴۷ (عقب‌مانده) |
| Seed | `ops/db.sh seed` | ۰ | پاس | — |
| SQL | `ops/db.sh test` | ۰ | **۱۱۱۱ ادعا / ۳۵ فایل** | ۱۰۹۷ / ۳۳ |
| Node — API | `pnpm -r test` | ۰ | **۵۸۸** pass · ۰ fail · ۰ skip · ۰ cancelled | — |
| Node — Web | `pnpm -r test` | ۰ | **۱۹۹** pass · ۰ fail · ۰ skip · ۰ cancelled | — |
| Node — جمع | | ۰ | **۷۸۷** | ۷۶۵ |
| افزونه PHP | `php …/payload-test.php` | ۰ | **۸۴ ادعا** | ۶۰ |
| Lint | `pnpm lint` (`eslint .`) | ۰ | پاس | — |
| Typecheck | `pnpm -r typecheck` | ۰ | پاس | — |
| زنجیرهٔ تأمین | `pnpm audit --prod --audit-level high` | ۰ | آسیب‌پذیری شناخته‌شده‌ای نیست | — |

**نکتهٔ اعتبار:** تست‌های یکپارچه با `DATABASE_URL` **ست‌شده** اجرا شدند، پس
`skipped = 0` واقعی است نه «سبزِ وانمودی».

**نکتهٔ PHP:** `stock-run-test.php` به‌تنهایی با کد ۲۵۵ می‌شکند، ولی این نقص نیست —
یک قطعهٔ `require`شده در خط ۴۹۲ `payload-test.php` است و ادعاهایش در همان ۸۴ شمرده
می‌شوند. (بررسی شد، گزارش نشد.)

## وضعیت فازها

| فاز | موضوع | وضعیت |
|---|---|---|
| ۰ | امکانات محیط + مبنا + خط پایه | **تمام** |
| ۱ | بازبینی PRهای ۴۷–۵۱ و ۵۲–۵۴ | **تمام** — هر هشت PR بازبینی شد |
| ۲ | ماتریس الزامات + ۱۴ معیار پذیرش | نشده |
| ۳ | یکپارچگی مالی + جهش + Property-based | **تمام** — ۱۰۵ جهش/۰ بازمانده · خصوصیت هزینه‌یابی ۱۵۰ سناریو |
| ۴ | تراکنش، هم‌زمانی، Idempotency، نقش DB | ۸٫۴ و ۸٫۵ **تمام** · Idempotency سراسری باقی |
| ۵ | هشت حوزهٔ اجباری | نشده |
| ۶ | امنیت + Threat Model + Fuzzing | Fuzzing مرز پول و ورودی (۸٫۶) **تمام** · Threat Model باقی |
| ۷ | یکپارچگی سایت | نشده |
| ۸ | بازیابی، ارتقا، عملیات | نشده |
| ۹ | یک روز کامل فروشگاه + عملکرد + UI/UX | نشده |
| ۱۰ | همگام‌سازی لحظه‌ای (Push) + ADR-007 | نشده |
| ۱۱ | Full Regression نهایی | نشده |
| ۱۲ | گزارش نهایی | نشده |

## موانع خارجی شناسایی‌شده در فاز ۰

| مانع | لازم برای | اثر | تلاش برای رفع |
|---|---|---|---|
| دیمن Docker اجرا نمی‌شود | محیط یک‌بارمصرف Compose | **رفع شد** — کلاستر محلی PostgreSQL 16 جایش را گرفت | موفق |
| WordPress/WooCommerce نصب نیست | بخش ۸٫۹، ۸٫۱۳، بند ۵۰ و ۶۱ | Integration واقعی سایت | در فاز ۷ تلاش می‌شود |
| کارت‌خوان فیزیکی و SDK | حوزهٔ «ب» | آزمون سخت‌افزاری | غیرممکن — مانع خارجی |
| بارکدخوان فیزیکی | تأیید نهایی برچسب | آزمون سخت‌افزاری | غیرممکن — مانع خارجی |

## یافته‌ها تا این لحظه

| شناسه | عنوان | شدت | وضعیت |
|---|---|---|---|
| FND-001 | اعداد تست در `CLAUDE.md`/`README.md` عقب‌اند | کم | **اصلاح شد** |
| FND-002 | «صف آفلاین» به هیچ گردش‌کاری وصل نیست | بالا | باز — نیازمند تصمیم دامنه |
| FND-003 | ۴۰۹ `in_flight`/۴۲۹/۴۰۸ در `isNetworkFailure` نیستند | کم (نهفته) | باز |
| FND-004 | سند افتتاحیه دو بار ثبت می‌شود · ارزش موجودی در دفتر گم می‌شود | **بحرانی** | **اصلاح شد** (مهاجرت ۰۴۹) |
| FND-005 | هیچ ثابتی دفتر را با ارزش واقعی انبار نمی‌سنجد | بالا | **اصلاح شد** (`inventory.ledger_check`) |
| FND-006 | مقدار ستون از راه `detail` خطای پستگرس به لاگ می‌رود | متوسط | **اصلاح شد** (سریالایزر `err`) |
| FND-007 | سفارش معوق ووکامرس خاموش نمی‌شد، پس سه ستون دیگر بی‌اثر بودند | متوسط | **اصلاح شد** |
| FND-008 | سازگاری HPOS اعلام نشده بود (هرچند کد سازگار است) | کم | **اصلاح شد** |
| FND-009 | «دو لایه دفاع» بند ۳ SECURITY.md یک لایه بود — نقش و REVOKEها وجود نداشتند | **بالا** | **اصلاح شد** — فعال‌سازی تصمیم مالک |
| FND-010 | سه قید معوق هرگز شلیک نمی‌کردند؛ هیچ تست SQL به COMMIT نمی‌رسید | متوسط | **اصلاح شد** (`db/test/deferred.sh`) |
| FND-011 | تلفن قلابی سفارش سایت، چند مشتری بی‌ربط را در یک پرونده ادغام می‌کرد | **بالا** | **اصلاح شد** |
| FND-012 | یک تست امنیتی گاه‌به‌گاه شکست می‌خورد (بند ۷۲) | متوسط | **اصلاح شد** |
| FND-013 | ادعای «جمع سود دو روش یکی است» حساب می‌شد، اندازه‌گیری نمی‌شد | متوسط | **اصلاح شد** — و ادعا **درست بود** |

## خط پایه پس از اصلاح — از صفر بازسازی شد

`ops/db.sh reset` + `migrate` + `seed` + کل مجموعه، همه با کد خروج ۰:

| مجموعه | عدد |
|---|---|
| ادعای SQL | **۱۱۳۱** (۳۸ فایل) |
| Node — API | **۵۹۱** pass · ۰ fail · ۰ skip · ۰ cancelled |
| Node — Web | **۱۹۹** pass · ۰ fail · ۰ skip · ۰ cancelled |
| افزونه PHP | **۱۰۲** |
| `pnpm check` (lint+typecheck+test) | کد خروج ۰ |
| `pnpm audit --prod --audit-level high` | پاک |

## نتیجه فاز ۱ — جدول بازبینی هشت PR

| PR | ادعای اصلاح | باگ اثبات شد؟ | ریشه‌ای؟ | پسرفت؟ | تست مقاوم؟ | یافتهٔ تازه |
|---|---|---|---|---|---|---|
| #47 | صف آفلاین بدون ACK حذف نشود | بله | بله | نه | **بله — ۴ جهش، هر ۴ گرفته شد** | FND-002، FND-003 |
| #48 | دامنهٔ شعبه در پیشنهاد سایز | بله | بله | نه | **بله — ۳ ادعا شکست** | — |
| #49 | Import هم‌زمان افتتاحیه | بله | **نیمه** — قفل انباری بود، باید شعبه‌ای باشد | نه | بله، ولی فقط همان انبار | **FND-004 (بحرانی)** |
| #50 | توکن فاکتور از لاگ | بله | بله برای خط درخواست | نه | بله | **FND-006** (خط خطا) |
| #51 | موجودی صفر ووکامرس | بله (سه باگ) | بله | نه | بله | **FND-007**، FND-008 |
| #52 | `@types/*` | — | — | نه | — | — |
| #53 | `fastify` 5.12.1→5.12.3 | — | — | نه | — | — |
| #54 | `typescript-eslint` 8.69→8.70 | — | — | نه | — | — |

سه PR Dependabot همه Patch/Dev هستند و مجموعهٔ کامل مالی را در همین SHA
اجرا کردم: سبز. (پرامپت می‌پرسید «آیا پس از آن‌ها دوباره اجرا شده‌اند؟» — حالا بله.)

زنجیرهٔ تأمین (بخش ۸٫۱۰) بررسی شد: هر ۴ اکشن به SHA کامل ۴۰ رقمی پین‌اند ·
`--frozen-lockfile` در CI · `pnpm-lock.yaml` کامیت شده · `pnpm audit --prod
--audit-level high` پاک · **هیچ `continue-on-error` و هیچ Path Filter نیست**
(بند ۶۹) · CI هر شش دروازه را اجرا می‌کند (`db.sh test`، `lint`، `typecheck`،
`test`، `audit`، تست PHP).

## نتیجهٔ بخش ۸٫۲ — تست جهش

۱۵ تابع · **۱۰۵ جهش** (هر نگهبان و هر `FOR UPDATE` جداگانه) · **۰ بازمانده**.
هیچ شکاف پوششی در نگهبان‌های هستهٔ مالی نیست.

⚠️ نسخهٔ اول ابزارِ خودم **۲۰ بازماندهٔ کاذب** گزارش کرد — چون `count=1` فقط
اولین `RAISE` را می‌زد و `concurrency.sh` را اجرا نمی‌کرد. کنترل مثبت (۰
پروندهٔ قرمز روی نسخهٔ سالم) و اصلاح هر دو ایراد، نتیجه را برگرداند.
ابزار: `scratchpad/mutate2.py` (بیرون از مخزن — ابزار ممیزی است، نه محصول).

## قدم بعدی دقیق

بخش ۸٫۱۲ — **ممیزی UI/UX و RTL با مرورگر واقعی**. Chromium موجود است
(`/opt/pw-browsers/chromium`). Build تولیدی ساخته شود، با CSP واقعی باز شود،
و هر صفحه در هفت عرض (۳۲۰ تا ۱۹۲۰) اسکرین‌شات بگیرد. گزارش مالک می‌گوید
رابط **چپ‌چین** دیده می‌شود ولی کد از خاصیت‌های منطقی استفاده می‌کند — این
تناقض باید با چشم داوری شود، نه با grep.

سپس ۸٫۱۳ (همگام‌سازی لحظه‌ای Push + ADR-007 — تأییدشده) و ۸٫۷ (یک روز
کامل فروشگاه).
