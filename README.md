# Label Mod Core

پلتفرم یکپارچه لیبل مد — حسابداری، انبار، خرید، فروش و صندوق فروشگاهی.

> **وضعیت بررسی در ۲۰۲۶/۰۹/۲۳:** هسته، API، رابط کاربری و افزونه ووکامرس آزمون خودکار دارند؛ سناریوهای مشخص حسابداری و اتصال روی محیط آزمایشی زنده نیز بررسی شده‌اند.
> عبور از CI به معنی تضمین نبود خطا یا تأیید همهٔ تجهیزات و سرویس‌های بیرونی نیست. محدودیت‌ها در بخش «آزمون و کارهای باقی‌مانده» آمده‌اند.
> استقرار: [راهنمای استقرار](docs/DEPLOYMENT.md)؛ پیش از بهره‌برداری، تصمیم‌های حسابداری و مالیاتی پایین این صفحه باید تأیید شوند.

[CI مخزن](https://github.com/AliJ021/labelmod-core/actions) · [اجرای سبز PR #90](https://github.com/AliJ021/labelmod-core/actions/runs/35875416639)

در اجرای مرجع بالا: ۷۲۶ تست API با نقش مالک، همان ۷۲۶ تست با نقش محدود، ۲۳۷ تست وب و ۴۸۰ سناریوی مرورگر گذشتند؛ هر چهار job موفق بودند. شمارش‌ها متعلق به همین اجرا هستند؛ برای commit جدید نتیجهٔ همان SHA را بررسی کنید.

---

## چه چیزی ساخته شده

### فضای کار وب و صندوق

رابط React/Vite فارسی و راست‌به‌چپ، ناوبری کناری دسکتاپ و ناوبری پایین موبایل دارد.
مسیرهای اصلی و زیرصفحه‌ها در نشانی مرورگر باقی می‌مانند. جست‌وجوی بخش‌ها با واژه‌هایی
مثل «بارکد»، «PIN»، «بکاپ» و «فروش کاربران» مسیر قابلیت مجاز را نشان می‌دهد.

- **صندوق:** یک ورودی «نام، کد یا بارکد محصول»؛ جست‌وجوی نام ابتدا محصول مادر و سپس
  رنگ و سایز را نشان می‌دهد. بارکد دقیق همان تنوع را اضافه می‌کند. موجودی هنگام افزودن،
  تغییر تعداد و دریافت وجه کنترل می‌شود؛ رزرو ایجاد نمی‌شود و کنترل نهایی تراکنشی باقی است.
- **فاکتورها:** پیش‌نویس‌های سرور، فیلتر وضعیت/شعبه/تاریخ، ثبت‌کننده و نهایی‌کننده، ادامهٔ
  پیش‌نویس مجاز و چاپ سند قطعی. هویت تاریخیِ بی‌شاهد «نامعلوم» می‌ماند.
- **گزارش‌ها:** فروش کاربران با مبنای نهایی‌کننده یا ثبت‌کننده و گزارش مستقل اسنپ‌پی.
- **اسنپ‌پی:** ثبت دستی پرداخت تأییدشده با شمارهٔ پیگیری؛ حساب تسویه باید در تنظیمات
  معتبر باشد. این قابلیت اتصال خودکار به درگاه اسنپ‌پی یا محاسبهٔ خودکار کارمزد نیست.
- **بکاپ:** مسیر مستقل در تنظیمات وجود دارد؛ بدون نصب سرویس مدیریتی جدا، صریحاً
  «متصل نشده» است. [مرز دسترسی و راهنمای بازیابی](docs/WEB-BACKUP-RESTORE.md)
  را پیش از فعال‌سازی بخوانید. هر بازیابی اصلی به تأیید همان نسخهٔ بکاپ نیاز دارد.

[ممیزی قابلیت‌ها و وضعیت پذیرش](docs/WEB-WORKSPACE-AUDIT.md) وضعیت آزمون‌ها و موارد
باقی‌مانده را جدا ثبت می‌کند. آزمون نرم‌افزار جای چاپ، اسکن، پرداخت و تحویل پیامک واقعی نیست.


راهنمای امکانات صندوق، پرداخت ترکیبی، موجودی محصول، PIN شخصی و چاپ لیبل:
[مسیرهای کاربری و آزمون تجهیزات](docs/POS-AND-DEVICE-ACCEPTANCE.md).
پیامک به‌صورت عامل دوم اختیاری اضافه شده است؛ فعال‌شدن آن به تنظیم سرویس
و تأیید شماره نیاز دارد: [تصمیم و محدودیت‌های امنیتی](docs/ADR-008-optional-sms-mfa.md).

### هسته مالی و انبار

| بخش | وضعیت |
| --- | --- |
| اسکیمای پایگاه داده (۸ اسکیما) | ✅ |
| انبار حرکت‌محور، با سه روش قیمت تمام‌شده | ✅ تست‌شده |
| رسید خرید با تخصیص هزینه حمل (Landed Cost) | ✅ تست‌شده |
| فروش با Snapshot قیمت و بهای تمام‌شده | ✅ تست‌شده |
| مرجوعی با بهای Snapshot فروش اصلی | ✅ تست‌شده |
| بستن شیفت و سند تجمیعی | ✅ تست‌شده |
| دفتر کل دوبل با توازن اجباری در سطح دیتابیس | ✅ تست‌شده |
| موتور ثبت داده‌محور (کدینگ قابل تغییر بدون کد) | ✅ تست‌شده |
| Idempotency و کنترل همزمانی | ✅ تست‌شده |
| لاگ حسابرسی با زنجیره هش | ✅ تست‌شده |
| خزانه: ثبت صندوق، بانک، کارت‌خوان و درگاه | ✅ منطق حسابداری تست‌شده؛ اتصال واقعی دستگاه/PSP جداگانه تأیید می‌شود |
| تسویه کارت‌خوان و درگاه با استنتاج کارمزد | ✅ تست‌شده |
| چک دریافتی و پرداختی — ماشین وضعیت کامل | ✅ تست‌شده |
| گردش حساب اشخاص | ✅ تست‌شده |

### لایه API — ساخته شده

Fastify + Kysely؛ احراز هویت با توکن مات، مجوزهای داده‌محور از
`identity.permission_rule` همراه کنترل نشست و دامنهٔ شعبه/انبار، و CSRF.
عملیات مالی تکرارپذیر قرارداد Idempotency دارند؛ ویرایش اعتبارنامهٔ ملی‌پیامک
با شمارهٔ ویرایش و رد تعارض هم‌زمان کنترل می‌شود. قرارداد هر مسیر را جدا بررسی کنید.

| گروه | مسیرها |
| --- | --- |
| احراز هویت | ورود، خروج، قفل، بازگشایی با PIN، احراز مجدد، ابطال همه نشست‌ها |
| دستگاه و نشست | **تأیید و ابطال دستگاه، فهرست نشست‌های باز، قطع دسترسی گوشی مفقودی** |
| خزانه و چک | **پرداخت، دریافت، هزینه، آورده، انتقال · ثبت و وصول و برگشت چک · سررسیدها** |
| فروش و صندوق | سبد، افزودن قلم، پرداخت، نهایی‌سازی، ابطال، باز و بستن شیفت |
| مرجوعی | پیش‌نمایش، برگ مرجوعی، ثبت، ابطال |
| دوره ثبت | درآمد ثبت‌نشده، بستن دوره کانال، **بستن خودکار سررسیدشده‌ها** |
| کالا | **ساخت و ویرایش کالا، بایگانی، تعیین و تغییر قیمت با تاریخچه**، ساخت خودکار تنوع‌ها، ماتریس موجودی، چاپ برچسب EAN-13 |
| خرید و انبار | تأمین‌کننده، سفارش خرید، رسید با هزینه حمل، برگشت از خرید، انبارگردانی |
| تنظیمات | خواندن با فراداده، تغییر با اعتبارسنجی، شرایط تسویه پایانه‌ها |
| دامنه کاربر | شعبه و انبار مجاز، روش‌های پرداخت فعال |
| سایت | ثبت سفارش ووکامرس، خوراک موجودی — با کلید API |
| عمومی | صفحه فاکتور مشتری با توکن — بدون نشست |

#### قرارداد صندوق — آنچه کلاینت لازم دارد

صندوق **هیچ شناسه‌ای را حدس نمی‌زند**: نه شعبه، نه انبار، نه کد روش
پرداخت. همه از سرور می‌آیند.

| مسیر | کار |
| --- | --- |
| `GET /branches` | شعبه‌های مجاز کاربر، با انبارهای فعال هرکدام و `kind` |
| `GET /payment-methods` | روش‌های فعال با `code`، نام فارسی، `kind`، `requiresRef` |
| `POST /invoices` | سبد تازه — با `Idempotency-Key`: ۲۰۱ تازه، ۲۰۰ Replay |
| `GET /invoices/:id` | فاکتور، **به‌علاوه `receivedAmount`** — تا سبدِ پس از Reload دوباره پول نگیرد |
| `GET /invoices/lookup` | یافتن فاکتور از روی **شماره رسید**، در دامنه شعبه |
| `GET /return-reasons` | علت‌های مجاز مرجوعی با برچسب فارسی |
| `GET /reports/daily` | فروش، وجه دریافتی و سود یک روز — سود پشت `cost.view` |
| `POST /invoices/:id/scan` | اسکن بارکد؛ کالای تکراری روی همان سطر شمرده می‌شود |
| `PATCH /invoices/:id/lines/:lineId` | تعداد **مطلق**، بدون بازقیمت‌گذاری |
| `PATCH .../lines/:lineId/discount` | تخفیف روی سطر موجود، بدون بازقیمت‌گذاری |
| `DELETE /invoices/:id/lines/:lineId` | حذف قلم |
| `POST /invoices/:id/payments` | `{ paymentId, replayed, receivedAmount, invoice }` |
| `POST /invoices/:id/finalize` | شماره فاکتور، Idempotent |
| `POST /shifts/:id/close` | شمارش کشو؛ Retry به‌جای ۴۰۹، Replay می‌شود |

چهار قاعده که کلاینت نمی‌تواند دورشان بزند:

- **قیمت هرگز از کلاینت نمی‌آید.** `scan` و `lines` فقط بارکد و تعداد
  می‌گیرند؛ `unit_price` از `catalog.price` خوانده می‌شود.
- **جمع‌ها فقط از پاسخ سرور.** `payableAmount` و `receivedAmount` هر دو
  در SQL حساب می‌شوند. `invoice.paidAmount` روی پیش‌نویس عمداً صفر است —
  آن ستون را `finalize_invoice` می‌نویسد.
- **`receivedAmount` مبلغ همین پرداخت نیست.** جمع **همه** پرداخت‌های
  موفق ثبت‌شده روی آن فاکتور است (`succeeded`، `settled`، `reconciled`)
  و از `sum()` سرور می‌آید، نه از مبلغی که کلاینت فرستاده. در Replay
  هم همین جمعِ لحظه پاسخ برمی‌گردد، نه عددِ لحظه پرداخت اول — یعنی
  اگر بعدش پرداخت دیگری ثبت شده باشد، Replay عدد بزرگ‌تر می‌دهد.
- **همان کلید با درخواست متفاوت، Replay نمی‌گیرد**: ۴۰۹
  `idempotency_key_reused`. کلید تکراری که بی‌صدا پاسخ عملیات دیگری را
  برگرداند، از نبودش بدتر است.

### رابط کاربری — بخشی

React + Vite، RTL کامل، زبان طراحی Liquid Glass ناحیه‌بندی‌شده (ADR-002).

| صفحه | وضعیت |
| --- | --- |
| تنظیمات و اعتبارنامهٔ ملی‌پیامک | ✅ وصل به API؛ کلید ذخیره‌شده بازنمایش داده نمی‌شود |
| انبار و خرید — سفارش، رسید، برگشت، انبارگردانی | ✅ وصل به API |
| ورود، قفل صفحه، بازگشایی با PIN | ✅ |
| داشبورد — فروش، وجه دریافتی، سود | ✅ وصل به API |
| صندوق — ورود، اسکن، سبد، پرداخت، نهایی‌سازی | ✅ وصل به API |
| مرجوعی — از روی شماره رسید | ✅ وصل به API |
| تخفیف سطر و ارتقای نشست با رمز | ✅ وصل به API |
| قیمت دستی روی سطر سبد — «مثل دشت» | ✅ وصل به API |
| بستن دوره کانال از داشبورد | ✅ وصل به API |
| گزارش‌ها — فروش، سود کالا، موجودی، کاردکس، دفتر حساب، تراز، اشخاص، مغایرت نقد | ✅ وصل به API |
| پنل مدیریتی — فروش ساعتی، مقایسه با ماه جلالی قبل، تحلیل سبد | ✅ وصل به API · فقط مدیر |
| انتقال بین انبارها — با بارکد، تغییرناپذیر پس از ثبت | ✅ وصل به API |
| پرسنل — ساخت، نقش، رمز، PIN، فعال و غیرفعال | ✅ وصل به API |
| پرونده مشتری — جست‌وجو، سقف اعتبار، رضایت، سابقه خرید | ✅ وصل به API |
| شناسنامه مشتری — نشانی، کد پستی، اندازه بدن (۱۳ کلید) | ✅ وصل به API |
| فصل کالا (گرم/سرد) و انبار آوتلت | ✅ وصل به API |
| مقصد کالای سالمِ برگشتی — قفسه یا آوتلت | ✅ وصل به API |
| خرید برای دیگری — گیرنده یک مشتری واقعی می‌شود | ✅ وصل به API |
| بسته هدیه — کاغذ، رنگ، گل، یادداشت، پنهان‌کردن قیمت | ✅ وصل به API |
| پیشنهاد سایز — حساب فاصله، نه AI | ✅ وصل به API |
| رجیستری درایور کارت‌خوان — افزودن و ویرایش مستندات SDK از تنظیمات | ✅ تست‌شده · ⚠️ هیچ درایوری پیاده نشده |
| PDF فاکتور — با چاپ مرورگر، بدون کتابخانه | ✅ در صفحه `/i/<توکن>` |
| پل پیام‌رسان (Webhook) | ✅ وصل به Outbox · ⚠️ اتصال به واتساپ/تلگرام کار همان سرویس است |
| صف امن فروش هنگام قطعی شبکه | ✅ با همان Idempotency-Key |
| ورود دومرحله‌ای TOTP + کد بازیابی | ✅ وصل به API |
| کلید امنیتی (WebAuthn / Passkey) — ثبت، ورود، حذف | ✅ وصل به API · ⚠️ مراسم واقعی هنوز با کلید فیزیکی آزموده نشده |
| موبایل — چیدمان واکنش‌گرا و بارکد با دوربین | ✅ اندروید و آیفون |
| خروجی CSV هر دوازده گزارش (سازگار با اکسل) | ✅ از همان مسیر، با `format=csv` |
| شماره مشتری پای صندوق — اختیاری | ✅ وصل به API |
| همگام‌سازی قیمت و موجودی با سایت، با کلید اتصال | ✅ وصل به API |
| خرید حضوری در حساب کاربری سایت | ✅ روی نصب آزمایشی ووکامرس بررسی‌شده؛ اعلان امضاشده پس از نهایی‌سازی، با Poll و WP-Cron برای جبران قطعی |

### افزونه ووکامرس — ساخته شده

`integrations/woocommerce/` — سفارش پرداخت‌شده را به سیستم می‌فرستد،
موجودی و قیمت سایت را از انبار می‌گیرد، و خرید حضوری را در حساب کاربری
مشتری نشان می‌دهد. راهنمای نصب فارسی همان‌جاست.

پنج چیز که در طراحی‌اش تعیین‌کننده بودند:

- **کلید API یک کاربر پشتی دارد، نه یک سیستم مجوز جدا.** مجوزش از همان
  `permission_rule` می‌آید و لاگ حسابرسی همان کاربر را می‌نویسد — پس
  معلوم است «سایت این را ثبت کرده». `web.refund` فقط پیام مرجوعی را در صف تأیید ثبت می‌کند.
  مجوز `web.refund.review` برای حسابدار/مدیر، همراه نشست انسانی کامل و مستقل از ارسال‌کننده،
  برای تأیید لازم است. تا آن زمان حتی برگ مالی پیش‌نویس ساخته نمی‌شود و موجودی تغییر نمی‌کند.
  در صفحهٔ مرجوعی، «درخواست‌های مرجوعی سایت» را باز کنید؛ تصمیم و دلیل آن حسابرسی می‌شود.
  تأیید، پرداخت بانکی تازه‌ای انجام نمی‌دهد؛ فقط بازپرداخت بررسی‌شده را در حسابداری ثبت می‌کند.
- **قیمت از سایت می‌آید — و فقط اینجا.** مشتری همان چیزی را پرداخت
  کرده که سایت نشانش داده؛ ثبت قیمت جاری فروشگاه یعنی درآمدی در دفتر
  که هرگز دریافت نشده. ولی از همان دروازه **قیمت دستی** می‌گذرد که
  صندوق از آن می‌گذرد، با دلیل ثابت «قیمت سایت».
- **اتصال با کلید است، نه با SKU و نه با نام.** نام کالا در سایت برای
  سئو نوشته می‌شود و عمداً با نام انبارداری فرق دارد؛ SKU هم می‌تواند
  عوض شود. خوراک `variationId` می‌فرستد و افزونه در متای
  `_lmc_variation_id` نگهش می‌دارد. SKU فقط یک پل **یک‌بارمصرف** برای
  برقراری اتصال اولیه است.
- **خرید حضوری در سایت `shop_order` نمی‌شود.** رکورد در نوع پست جدای
  `lmc_instore_purchase` می‌نشیند و با برچسب «سفارش حضوری» دیده می‌شود.
  `shop_order` یعنی موجودی دوباره کم شود، ایمیل برود، در گزارش سایت
  دوباره شمرده شود، و یک **حلقه بازگشتی** بسته شود چون همان Hookهایی
  را می‌زند که خودِ افزونه به آن‌ها گوش می‌دهد.
- **ارسال هرگز Checkout را کند نمی‌کند.** ناهم‌زمان است و با Backoff
  دوباره تلاش می‌کند. تکرار بی‌خطر است: کلید Idempotency شماره سفارش
  ووکامرس است، پس دو Webhook یک فاکتور می‌سازد.

⚠️ **واحد پول را با یک مبلغ معلوم تطبیق دهید:** سیستم ریال نگه
می‌دارد و بیشتر سایت‌ها تومان. غلط بودنش هیچ خطایی نمی‌دهد، فقط همه
مبالغ یک صفر کم یا زیاد می‌گیرند.

### Worker — پیامک و هشدار سررسید چک

`apps/api/src/worker/` — همان ایمیج API با دستور دیگر (ADR-001:
«Worker: همان کدبیس»). فرآیندش جداست چون ارسال پیامک ثانیه‌ها طول
می‌کشد و نباید در Event Loopی بنشیند که فاکتور صندوق را جواب می‌دهد.

| موضوع | چه می‌کند |
| --- | --- |
| `invoice.finalized` | پیامک «فاکتور ثبت شد» با **لینک فاکتور** |
| `cheque.due` | هشدار سررسید چک به موبایل مدیر، روزی یک بار برای هر چک |

**پیامک ضمیمه نمی‌گیرد، پس PDF ساخته نمی‌شود — لینک می‌رود.** صفحه
`/i/<توکن>` را مرورگر خودش «ذخیره به PDF» می‌کند، با فونت فارسی درست.
کتابخانه PDF رد شد: شکل‌دهی حروف فارسی در کتابخانه‌های PDF جاوااسکریپت
یا نیست یا شکسته است، و فاکتوری که حروفش جدا چاپ شود بدتر از نبودنش
است — به‌علاوه یک وابستگی تازه با هزینه زنجیره تأمین.

توکن ۲۴ بایت تصادفی است و **Idempotent**: لینکی که یک بار پیامک شده
عوض نمی‌شود. صفحه فقط فاکتور نهایی‌شده را نشان می‌دهد — پیش‌نویس،
فاکتور باطل و توکن غلط همه یک پاسخ می‌گیرند.

سه چیز که صف را از یک نشت خاموش جدا می‌کنند:

- **اجاره، نه قفل.** Workerی که وسط ارسال بمیرد پیام را در `sending`
  جا می‌گذارد؛ پس از پایان مهلت دوباره برداشته می‌شود. تحویل «حداقل
  یک بار» است و Handlerها همین‌طور نوشته شده‌اند.
- **نامه مرده، نه Retry بی‌پایان.** شماره غلط با تلاش صدم هم درست
  نمی‌شود. `platform.outbox_dead` و `ops/deploy.sh status` نشانش
  می‌دهند — همان نقشی که «درآمد ثبت‌نشده» برای دفتر دارد.
- **خاموش‌بودن یک شکست نیست.** فروش ناشناس، پیامک خاموش و مشتری بدون
  رضایت همه **موفق** بسته می‌شوند. اگر ناموفق می‌شدند، نامه مرده پر
  می‌شد از چیزهایی که قرار نبود بروند و خطای واقعی همان‌جا گم می‌شد.

⚠️ پیش‌فرض سرویس‌دهنده `log` است: هیچ پیامکی واقعاً نمی‌رود؛ متن پیام، شماره و لینک حامل توکن
در لاگ این آداپتور نوشته نمی‌شوند. Webhook روشن به `NOTIFY_WEBHOOK_TOKEN` نیاز دارد و Redirect را دنبال نمی‌کند.
اولین پیامک واقعی یک **تغییر تنظیم** است، نه یک
استقرار.

### مهاجرت داده — ابزارش آماده است

`apps/api/src/import/` و راهنمای فارسی در `docs/DATA-MIGRATION.md`.
پنج فایل CSV با ستون‌های ثابت: کالا، مشتری، تأمین‌کننده، موجودی اول
دوره، مانده‌های افتتاحیه. نمونه‌ها در `docs/import-samples/`.

**این یک «موتور Import عمومی» نیست و عمداً نیست** (فهرست «چه چیزی
نساز»): نگاشت دلخواه ستون‌ها یعنی هر بار کسی تصمیم می‌گیرد کدام ستون
کجا می‌رود، و آن تصمیم جایی ثبت نمی‌شود.

چهار قاعده:

- **پیش‌فرض آزمایشی است.** بدون `--commit` هیچ‌چیز نوشته نمی‌شود. کاری
  که یک بار انجام می‌شود و برگرداندنش سخت است، باید اول دیده شود.
- **همه یا هیچ.** پنج فایل با هم سنجیده می‌شوند و در **یک تراکنش**
  نوشته. وارداتِ نیمه‌کاره از واردات‌نشده بدتر است.
- **سرمایه رقم متوازن‌کننده است**، نه یک ورودی: دارایی منهای بدهی.
  ابزار عدد را نشان می‌دهد و می‌گوید پیش از `--commit` با حسابدار
  تأییدش کنید.
- **اجرای دوباره امن است.** موجودی دو برابر نمی‌شود، کالای موجود
  به‌روز نمی‌شود، و سند افتتاحیه دوم اولی را معکوس می‌کند نه اینکه
  رویش بنشیند.

⚠️ **بهای تمام‌شده موجودی اجباری است.** موجودی بدون بها یعنی اولین
فروشِ آن کالا سودِ صددرصد نشان می‌دهد. اگر بها معلوم نیست، مهاجرت
می‌ایستد تا کسی تصمیم بگیرد — نه اینکه صفر فرض کند.

⚠️ **تاریخچه فاکتورهای قبلی منتقل نمی‌شود.** سند حسابداریِ ساخته‌شده
در یک سیستم دیگر، در این دفتر نه قابل حسابرسی است و نه قابل اعتماد.
مانده می‌آید، نه گردش.

---

## قابلیت‌هایی که ارزش دانستن دارند

**۱. بهای تمام‌شده بدون رانش گرد کردن.** به‌جای نگهداری `avg_cost`،
`total_value` نگه داشته می‌شود و میانگین لحظه‌ای محاسبه می‌شود. هنگام
تخلیه کامل موجودی، آخرین واحدها کل ارزش باقی‌مانده را جذب می‌کنند.
نتیجه: **دفتر کل و انبار هیچ‌وقت — حتی یک ریال — از هم جدا نمی‌افتند.**

**۲. سه روش قیمت تمام‌شده، یک کلید.** «آخرین قیمت خرید» (پیش‌فرض) کل
موجودی را با هر خرید به نرخ تازه ارزیابی می‌کند و تفاوت را در حساب
۵۱۰۲ می‌نشاند؛ «میانگین موزون» رفتار کلاسیک را می‌دهد؛ **FIFO** هر
فروش را از قدیمی‌ترین خریدِ باقی‌مانده برمی‌دارد و برای این کار
لایه بهای تمام‌شده نگه می‌دارد. **جمع سود با هر سه یکی است، فقط زمان
شناسایی فرق می‌کند** — و تست همین برابری را ادعا می‌کند.

⚠️ از این سه، فقط میانگین موزون و FIFO در فهرست استاندارد حسابداری
شماره ۸ ایران‌اند. جزئیات: ADR-006.

**۳. قیمت دستی، بدون دو قیمت روی فاکتور.** قیمت تازه در `unit_price`
می‌نشیند و قیمت فهرست در `list_price` — فاکتور یک عدد نشان می‌دهد.
سقف از **کاهش کل** سنجیده می‌شود (تخفیف + تفاوت قیمت)، وگرنه قیمت
دستی راه دور زدن سقف تخفیف بود.

قیمت سطری که قبلاً دستکاری شده هم از همان سقف می‌گذرد: `list_price`
**یک بار** Snapshot می‌شود و تغییر دوم دست‌نخورده نگهش می‌دارد. بدون
این، دو تغییر پیاپی (۱۰٪ و بعد ۲۲٪ نسبت به قیمت تازه) کاهش واقعی ۳۰٪
را از سقف رد می‌کرد. برگرداندن قیمت به فهرست، Snapshot و دلیل را پاک
می‌کند و سطر دوباره «دست‌نخورده» می‌شود.

**۴. چک، تا وصول نشود، پول نیست.** هر پله حساب خودش را دارد: نزد ما
(۱۵۰۱)، در جریان وصول (۱۵۰۲)، برگشتی (۱۵۰۳)، اسناد پرداختنی (۲۴۰۱).
وضعیت برگه یک Projection از زنجیره رویداد است و `UPDATE` مستقیم رویش
رد می‌شود.

**۵. تصمیم‌های باز، داده‌اند نه کد.** کدینگ حساب در `ledger.account`،
نگاشت رویداد به سند در `ledger.posting_rule`، سقف دسترسی در
`identity.permission_rule`، و تنظیمات معمول در `platform.setting` —
هرکدام با نوع، برچسب فارسی، گزینه‌ها، بازه مجاز و مجوز لازم برای
تغییرش. صفحه تنظیمات از همین فراداده ساخته می‌شود، پس تنظیم تازه بدون
یک خط کد UI دیده می‌شود.

**۶. «امروز» یک تعریف دارد.** `platform.business_date()` از تنظیم
`platform.timezone` می‌آید، نه از منطقه زمانی سرور. بدون این، سفارش
یک بامداد روی سروری با UTC به روز قبل می‌خورد.

---

## راه‌اندازی برای توسعه

```bash
cp .env.example .env      # رمز پایگاه داده را عوض کنید
docker compose up -d db

export DATABASE_URL='postgres://labelmod:رمز@localhost:5432/labelmod'
ops/db.sh migrate         # ساخت اسکیما
ops/db.sh seed            # فقط پایگاه دادهٔ تازه و جداگانهٔ توسعه
ops/db.sh test            # آزمون‌های SQL روی دیتابیس آزمایشی

corepack enable
pnpm install --frozen-lockfile
pnpm check                # lint + typecheck + تست‌های API و وب
pnpm --filter @labelmod/api dev
pnpm --filter @labelmod/web dev

ops/install-hooks.sh      # یک بار روی هر کلون؛ مکمل CI و حفاظت شاخه
```

اگر `ops/db.sh test` سبز نشد، هیچ کار دیگری نکنید تا علتش پیدا شود.

**`seed` هیچ حساب انسانی نمی‌سازد** و نباید بسازد — رمز پیش‌فرضِ
کامیت‌شده همان چیزی است که بند ۸ SECURITY.md ممنوع کرده. اولین کاربر:

```bash
node --experimental-strip-types apps/api/src/cli/create-user.ts \
     --username ali --name 'علی جوادی' --role admin
```

رمز تصادفی ساخته می‌شود و **یک بار** چاپ می‌شود.

---

## استقرار روی سرور

راهنمای کامل و گام‌به‌گام: **`docs/DEPLOYMENT.md`**

**نمونهٔ زیر فقط برای نصب تازه روی دیتابیس خالی است.** برای سرور دارای داده،
`.env` را جایگزین نکنید و `seed` اجرا نکنید؛ ابتدا بکاپ و بازیابی روی نسخهٔ جدا،
سپس مهاجرت‌ها و مجوزهای نقش برنامه را طبق راهنمای استقرار اعمال کنید.

```bash
cp .env.example .env      # DB_PASSWORD · APP_DB_PASSWORD مستقل · SITE_ADDRESS · ACME_EMAIL
docker compose -f docker-compose.prod.yml up -d db
ops/deploy.sh migrate
ops/deploy.sh seed        # فقط نصب تازه؛ روی دفتر موجود اجرا نشود
ops/deploy.sh roles       # نقش APP_DB_USER؛ پیش‌فرض labelmod_app
ops/deploy.sh up          # API و Worker فقط با نقش محدود بالا می‌آیند
ops/deploy.sh user --username ali --name 'علی جوادی' --role admin
ops/deploy.sh status
```

سرویس‌های `docker-compose.prod.yml`:

| سرویس | کار | پورت روی میزبان |
| --- | --- | --- |
| `db` | PostgreSQL 16 | **هیچ** — فقط شبکه داخلی |
| `api` | Fastify | **هیچ** — فقط از پشت Caddy |
| `caddy` | TLS خودکار + سرو UI + Reverse Proxy روی `/api` | ۸۰، ۴۴۳ |
| `scheduler` | بستن شبانه دوره فروش سایت، بکاپ | — |
| `worker` | پردازش Outbox، پیامک و Push موجودی/قیمت/خرید حضوری | **هیچ** |

سه چیزی که ارزش دانستن دارند:

**۱. یک دامنه برای هر دو.** کوکی نشست `SameSite=Strict` است و UI مسیر
`/api` را نسبی صدا می‌زند. دو دامنه یعنی کوکی اصلاً فرستاده نمی‌شود و
ورود بی‌هیچ پیام مفیدی کار نمی‌کند.

**۲. `wasm-unsafe-eval` در CSP اختیاری نیست.** اسکنر بارکد روی هر
مرورگری بدون `BarcodeDetector` بومی — **از جمله آیفون** — از
WebAssembly استفاده می‌کند. بدون آن کلیدواژه، دوربین باز می‌شود و هیچ
بارکدی خوانده نمی‌شود، بی‌آنکه خطایی بدهد. `apps/web/test/csp.test.ts`
این را در CI قفل کرده.

**۳. کار شبانه بدون cron.** سرویس `scheduler` هر ساعت بیدار می‌شود؛
اگر کار امروز انجام نشده باشد اجرایش می‌کند و نشانه‌اش را در یک Volume
می‌گذارد — پس Restart ظرف، اجرای دوباره نمی‌سازد.
**موجودی ربطی به بستن دوره ندارد و همان لحظه فروش کم می‌شود.**

کنترل روزانه: `SELECT * FROM sales.unposted_revenue;` باید خالی باشد.

⚠️ بکاپ خودکار روی همان سرور می‌نشیند. تا وقتی رمزنگاری‌شده به بیرون
کپی نشود و Restore آن یک بار تست نشود، **بکاپ نیست** — بند ۶ راهنمای
استقرار.

**تمرین بازیابی یک رکورد است، نه یک جمله.** `ops/restore-drill.sh` دامپ
را در یک دیتابیس یک‌بارمصرف برمی‌گرداند، یازده ادعا رویش می‌راند — از
جمله زنجیره حسابرسی حلقه‌به‌حلقه، خواندن مانده انبار با جمع حرکت‌ها، و
خواندن ارزش موجودی در دفتر با ارزش واقعی انبار (مهاجرت ۰۴۹) —
و نتیجه را در `platform.restore_drill` ثبت می‌کند.
`ops/deploy.sh status` عبور از `backup.restore_drill_days` را
`overdue` نشان می‌دهد؛ نبودِ هر تمرینی `never` است.

⚠️ تمرین **ناموفق** زنگ را خاموش نمی‌کند — وگرنه کافی بود هر شب اجرا
شود و شکست بخورد تا هیچ‌کس هرگز هشداری نبیند.

---

## اصولی که در کد اجبار شده‌اند

این‌ها توصیه نیستند — دیتابیس آن‌ها را رد می‌کند:

- **هر سند حسابداری باید متوازن باشد** — `CONSTRAINT TRIGGER` معوق.
- **حرکت انبار و لاگ حسابرسی تغییرناپذیرند.** اصلاح فقط با حرکت معکوس.
- **سند تأییدشده/نهایی بازنویسی یا حذف نمی‌شود.** پس از تراکنش ساخت، افزودن یا جابه‌جایی سطر هم ممنوع است؛ [جزئیات نگهبان‌ها](docs/JOURNAL-GUARDS.md).
- **موجودی منفی نمی‌شود** مگر با مجوز صریح.
- **پول هرگز float نیست.** `NUMERIC(18,0)` ریالی در SQL، `bigint` در
  TypeScript، **رشته** در JSON.
- **فروش هم‌زمان اثر تکراری نمی‌سازد.** قفل بدبینانه روی سطر موجودی.
- **شماره فاکتور فقط در لحظه Commit تخصیص می‌یابد**، بدون پرش.
- **مقدار تنظیم فقط از `platform.set_setting()` عوض می‌شود** —
  با اعتبارسنجی نوع و بازه، و ردّ حسابرسی با مقدار پیش و پس.
- **کارمزد و دوره تسویه فقط از `treasury.set_settlement_terms()`.**
- **کاهش قیمت بالای آستانه بدون دلیل ثبت نمی‌شود** — Trigger روی
  `invoice_line`، نه لایه API.
- **PIN عملیات حساس را باز نمی‌کند** — فهرستش داده است، و در ادعای
  پایدار CI قفل شده.

---

## ⚠️ تصمیم‌های باز — پیش از ورود داده واقعی

| تصمیم | مقدار فعلی | وضعیت |
| --- | --- | --- |
| روش بهای تمام‌شده | **آخرین قیمت خرید** | ✅ تصمیم مالک · ⚠️ پیش از اظهارنامه با حسابدار چک شود |
| مهلت مرجوعی | **۴۸ ساعت** | ✅ تصمیم مالک |
| منطقه زمانی | **Asia/Tehran** | ✅ تصمیم مالک · تنها تعریف «امروز» در کل سیستم |
| دوره تسویه کارت‌خوان | **۱ روز** | ✅ تصمیم مالک |
| بستن دوره کانال آنلاین | **خودکار، شبانه** | ✅ تصمیم مالک |
| سیاست PIN صندوق‌دار | ۱۲ عملیات ممنوع | ✅ قابل تغییر در تنظیمات |
| کارمزد کارت‌خوان | ۰٪ | ⏳ منتظر صورتحساب PSP — از صفحه «پایانه‌ها»، به‌ازای هر پایانه |
| تجمیع سند فروش | **شیفتی** | ✅ تصمیم مالک · «هر فاکتور» پیاده نشده — تصمیم معماری، بند پایین |
| کدینگ حساب | ۵۸ حساب، **چهارسطحی** | ⏳ حسابدار تأیید کند — از صفحه «کدینگ حساب» ویرایش می‌شود · برگهٔ تطبیق با هلو/دشت: `docs/ACCOUNT-MAPPING-HOLOO-DASHT.md` |
| نگاشت حساب (درآمد و COGS به کدام حساب) | ۸۳ قاعده | ✅ قابل تغییر از صفحه «نگاشت حساب» — مجوز `ledger.mapping` |
| روش بهای تمام‌شده | **آخرین قیمت خرید** از سه گزینه | ⏳ حسابدار تأیید کند — از صفحه تنظیمات، گروه «انبار» |
| هشدار خودکار سلامت سیستم | **خاموش** | ✅ قابل روشن‌کردن از تنظیمات، گروه «اعلان» |
| نرخ ارزش افزوده | ۱۰٪ و **خاموش** | ⏳ مشاور مالیاتی |
| صورتحساب الکترونیکی | `none` | ⏳ مشاور مالیاتی |
| سقف تخفیف نقش‌ها | ۱۰٪ صندوق‌دار، ۲۵٪ سرپرست | ✅ قابل تغییر از صفحه «مجوزها» |
| دسترسی به تحلیل سبد | فقط **مدیر** | ✅ قابل تغییر از صفحه «مجوزها» |

همه از **صفحه تنظیمات** عوض می‌شوند — بدون psql، بدون Deploy.

**تنها تصمیم معماری باز: «یک سند به‌ازای هر فاکتور».** امروز فقط سند
تجمیعی شیفتی پیاده شده. تا مهاجرت ۰۲۴ گزینه‌اش در تنظیمات بود ولی
**هیچ کدی نمی‌خواندش** — انتخابش هیچ اثری نداشت، پس برداشته شد.

پیاده‌سازی‌اش کدِ نانوشته نیست، تصمیمی است که ADR-003 جور دیگری گرفته:
`ledger.posting_batch` دو ستون تکی `sale_entry_id` و `cogs_entry_id`
دارد و کل Idempotency روی همان‌هاست. با یک سند به‌ازای هر فاکتور، N
سند ساخته می‌شود و آن دو ستون کافی نیستند. لازمه‌اش یک جدول پیوند
تازه، بازنویسی `sales.post_batch()` و بازنگری در `unposted_revenue`
است — یعنی یک ADR، نه یک مهاجرت.

فهرست کامل: `SELECT key, value, description FROM platform.setting WHERE requires_approval;`

### الزام عملیاتی که فراموش‌شدنی است

**سررسید چک.** Worker حالا هشدارش را می‌فرستد (اگر
`notify.cheque_due_sms` روشن و موبایل مدیر پر باشد) — ولی **هیچ چیز
خودکار چک را وصول نمی‌کند.** هشدار یعنی «برو بانک»، نه اینکه کاری
انجام شده باشد.

`SELECT * FROM treasury.cheque_due WHERE urgency <> 'future';` همچنان
مرجع است. چکی که سررسیدش گذشته و هنوز `in_hand` است، یعنی کسی آن را به
بانک نبرده.

**پیام‌های نرفته.** `SELECT * FROM platform.outbox_dead;` باید خالی
باشد. خالی‌نبودنش یعنی پیامکی هست که پس از سقف تلاش هرگز نرفت —
`ops/deploy.sh status` هم نشانش می‌دهد.

---

## آزمون و کارهای باقی‌مانده

| | |
| --- | --- |
| Monitoring حرفه‌ای (Prometheus، Grafana) | `ops/deploy.sh status` و صفحهٔ «سلامت سیستم» هست؛ داشبورد متریک و Alertmanager نه |
| کد ارتباط با کارت‌خوان | ظرفش هست و **از تنظیمات پر و عوض می‌شود**؛ تا SDK نرسد، کدِ Handler نوشته نمی‌شود |
| Push و خرید حضوری | روی نصب آزمایشی واقعی ووکامرس بررسی شد. در سناریوی پنج خرید طی ده دقیقه، مشاهدهٔ موجودی حدود ۴ تا ۶٫۶ ثانیه پس از نهایی‌سازی بود؛ این عدد SLA نیست. تاریخچهٔ خرید اکنون اعلان امضاشدهٔ مستقل دارد؛ Worker با فاصلهٔ پیش‌فرض ۵ ثانیه کار می‌کند و Poll پانزده‌دقیقه‌ای پشتیبان باقی است. تأخیر صفر تضمین نمی‌شود؛ جزئیات در docs/INSTORE-PUSH.md. پوشش همهٔ تنظیمات HPOS ادعا نمی‌شود. |
| ملی‌پیامک | قرارداد API، حفاظت کلید، خطاها و فرم تنظیمات آزموده شدند؛ ارسال واقعی، اعتبار خط و تحویل گوشی به درخواست مالک به بعد موکول شده است. |
| سخت‌افزار و سرویس‌های بیرونی | چاپ واقعی MEVA TP-UNW روی رایانهٔ صندوق، بارکدخوان، تراکنش واقعی درگاه/کارت‌خوان و سرویس مالیاتی جداگانه تأیید می‌شوند. |
| اختتام و بازگشایی سال | نگهبان ثبت در سال بسته و تغییرناپذیری دفتر پیاده شده؛ گردش کامل اختتام/بازگشایی با این نگهبان‌ها پیاده‌سازی نمی‌شود. |

**عمداً ساخته نشده:** «چک به‌عنوان روش پرداخت داخل فاکتور فروش». چک
سند مستقل دارد و بدهی مشتری را تسویه می‌کند؛ گنجاندنش در tender
فاکتور رفتار هسته فروش را عوض می‌کند. یک تصمیم جداگانه، پس از تأیید
حسابدار.

Microservice · Redis · Kubernetes · حالت آفلاین کامل · Windows Bridge ·
اتصال مستقیم به سامانه مالیاتی · موتور Import عمومی · AI پیشنهاد سایز.

---

## ساختار

```
db/migrations/   اسکیما و توابع — شماره‌دار، SQL خام، قابل حسابرسی
db/seed/         کدینگ حساب، قواعد ثبت، تنظیمات، داده مرجع
db/test/         ۴۶ فایل تست مالی — در CI و pre-push اجرا می‌شوند
db/reconcile/    تطبیق پایانی «یک روز کامل» — از تست E2E صدا زده می‌شود
docs/            ADR-001 تا ADR-007 · SECURITY.md · DEPLOYMENT.md
                 JOURNAL-GUARDS.md · MELIPAYAMAK.md
ops/             db.sh · deploy.sh · install-hooks.sh · close-due-days.sh
                 restore-drill.sh — بکاپ را واقعاً برمی‌گرداند
                 deploy/ — Caddyfile، زمان‌بند شبانه
integrations/    افزونه ووکامرس (PHP) — سفارش سایت و خوراک موجودی
apps/api/        Fastify + Kysely — احراز هویت، نشست، مجوز، فروش، صندوق،
                 مرجوعی، دوره ثبت، کالا، سفارش و رسید خرید، برگشت
                 از خرید، انبارگردانی، گزارش روزانه، تنظیمات
                 src/worker/ — صف Outbox، پیامک، هشدار سررسید چک
                 src/import/ — مهاجرت داده از سیستم فعلی
apps/web/        React + Vite — ورود و قفل صفحه، صندوق، مرجوعی، داشبورد،
                 تنظیمات (کلیدها، کدینگ حساب، پایانه‌ها، مجوزها،
                 افتتاحیه و تفصیلی).
                 همه به API وصل‌اند؛ روی موبایل بارکد با دوربین
```

| سند | موضوع |
| --- | --- |
| ADR-001 | چرا این Stack، و چرا منطق مالی در دیتابیس |
| ADR-002 | زبان طراحی Liquid Glass ناحیه‌بندی‌شده و پالت قفل‌شده |
| ADR-003 | چرا دوره ثبت از شیفت صندوق جداست |
| ADR-004 | چرا چک یک سند مستقل است، نه یک روش پرداخت |
| ADR-005 | اعتماد دستگاه، ارتقای نشست و CSRF |
| ADR-006 | آخرین قیمت خرید، تجدید ارزیابی، و اینکه سود کجا شناسایی می‌شود |
| SECURITY.md | Argon2id، نشست مات، WebAuthn، زنجیره تأمین |
| DEPLOYMENT.md | استقرار روی سرور، TLS، CSP، بکاپ، عیب‌یابی |
| DATA-MIGRATION.md | انتقال کالا، مشتری، موجودی و مانده‌های افتتاحیه |
| [JOURNAL-GUARDS.md](docs/JOURNAL-GUARDS.md) | قفل سال، حفاظت هدر/سطر سند و محدودیت اختتام |
| [MELIPAYAMAK.md](docs/MELIPAYAMAK.md) | تنظیم کلید، رمزنگاری، بکاپ کلید اصلی و حدود آزمون پیامک |

## اجرای رگرسیون

فرمان‌های زیر برای محیط توسعه/CI با PostgreSQL جداگانه‌اند؛ `DATABASE_URL`
نباید به دفتر اصلی اشاره کند. پیش‌نیازها و متغیرهای محیط در
[Workflow](.github/workflows/db-tests.yml) و [راهنمای استقرار](docs/DEPLOYMENT.md) هستند.

```bash
ops/db.sh test
pnpm check
LMC_TEST_DB_ROLE=app pnpm --filter @labelmod/api test
pnpm --filter @labelmod/web build
pnpm --filter @labelmod/web exec playwright install --with-deps chromium webkit
pnpm --filter @labelmod/web test:e2e
php integrations/woocommerce/test/payload-test.php
php integrations/woocommerce/test/push-receiver-test.php
php integrations/woocommerce/test/refund-test.php
php integrations/woocommerce/test/instore-test.php
pnpm audit --prod --audit-level high
```

CI علاوه بر این‌ها نحو PHP، ساخت سه ایمیج و کنترل‌های پیکربندی استقرار را بررسی می‌کند.
تست قرمز باید با اصلاح علت رفع شود؛ حذف، Skip یا ضعیف‌کردن assertion برای سبزشدن CI مجاز نیست.

## قدم بعدی

۱. **یک بار با بارکدخوان اکسیوم واقعی و روی HTTPS آزمایش شود** —
   آستانه تشخیص اسکنر (`MAX_GAP_MS` در `apps/web/src/lib/scanner.ts`)
   از روی مشخصات بازار است نه دستگاه شما، و دوربین موبایل روی `http://`
   اصلاً اجازه کار ندارد. هیچ تستی جای این دو را نمی‌گیرد.
۲. تصمیم‌های ⏳ جدول بالا با حسابدار و مشاور مالیاتی قفل شوند.
۳. پیش از واردات بعدی، فایل‌های منبع و مانده‌های افتتاحیه با حسابدار تطبیق و روی نسخهٔ جدا تمرین شوند؛ [راهنمای مهاجرت](docs/DATA-MIGRATION.md).
۴. شمارش فیزیکی نمونه‌ای و تطبیق ارزش انبار با دفتر.
۵. اجرای موازی با سیستم فعلی تا سه روز بدون مغایرت.


### نگهبان‌های دوره و اسناد ثبت‌شده

مهاجرت‌های ۰۶۹ و ۰۷۰ ثبت سند و تغییر وضعیت سال را با قفل مشترک سال مرتب می‌کنند. سند تأییدشده به پیش‌نویس برنمی‌گردد و محتوای هدرش بازنویسی نمی‌شود؛ فقط ارتقای بدون تغییر محتوای confirmed به final مجاز است. سطرها فقط در تراکنش ساخت سند تکمیل می‌شوند و پس از پایان آن تراکنش، افزودن سطر متوازن هم ممنوع است. انتقال سطر از پیش‌نویس به سند تأییدشده کنترل می‌شود. جزئیات و مرز این تغییر در [نگهبان‌های دفتر](docs/JOURNAL-GUARDS.md) است. این نگهبان‌ها، گردش کامل اختتام و بازگشایی سال را پیاده‌سازی نمی‌کنند.


### ملی‌پیامک

سرویس‌دهندهٔ `melipayamak` برای API کلیددار اضافه شده است. کلید و نام اختیاری حساب از تنظیمات اعلان‌ها قابل تغییرند؛ کلید به‌صورت AES-256-GCM با کلید اصلی محیط `SMS_CREDENTIAL_KEY` ذخیره می‌شود و در پاسخ یا حسابرسی برنمی‌گردد. گزینهٔ جدید بدون تغییر سرویس فعلی یا فعال‌کردن ارسال وارد تنظیمات می‌شود. پاسخ HTTP 200 به‌تنهایی موفقیت نیست: شناسهٔ ارسال مثبت و نبود خطای سرویس لازم است. راه‌اندازی، حریم خصوصی و محدودیت تحویل حداقل یک‌بار در [راهنمای ملی‌پیامک](docs/MELIPAYAMAK.md) آمده است.
