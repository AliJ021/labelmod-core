# Label Mod Core

سیستم یکپارچه حسابداری، انبار و فروش فروشگاه پوشاک «لیبل مد».
یک شعبه حضوری + سایت WooCommerce. جایگزین کامل نرم‌افزار حسابداری فعلی.

**به فارسی پاسخ بده.** کامنت‌های کد و پیام‌های خطای کاربر هم فارسی‌اند.

## قاعده حاکم

این سیستم پول و اسناد مالی یک کسب‌وکار واقعی را نگه می‌دارد.
**هرجا راحتی و صحت مالی تعارض دارند، صحت برنده است.**

## قواعد غیرقابل مذاکره

این‌ها در دیتابیس اجبار شده‌اند، نه فقط توصیه. اگر کدی بنویسی که آن‌ها را
نقض کند، دیتابیس ردش می‌کند — پس اول اینجا را بخوان:

- **پول هرگز float نیست.** `NUMERIC(18,0)` ریالی در SQL، `bigint` در
  TypeScript، **رشته** در JSON. نمایش تومان فقط در لایه UI.
- **موجودی فقط از `inventory.apply_movement()` تغییر می‌کند.** هیچ‌جا
  `INSERT` مستقیم در `stock_movement` و هیچ‌جا نوشتن مستقیم عدد موجودی.
- **سند حسابداری فقط از `ledger.post_entry()` ساخته می‌شود.** توازن با
  `CONSTRAINT TRIGGER` معوق بررسی می‌شود.
- **حرکت انبار و لاگ حسابرسی تغییرناپذیرند.** `UPDATE`/`DELETE` خطا می‌دهد.
  اصلاح فقط با حرکت یا سند معکوس.
- **حرکت انبار بی سند مرجع ثبت نمی‌شود** — مهاجرت ۰۵۹، معیار پذیرش ۲.
  `ref_type` و `ref_id` هر دو اجباری‌اند و **`opening` تنها استثناست**:
  واردات اولیه خودش مبدأ است و سند افتتاحیهٔ دفتر **پس از** حرکت‌ها
  ساخته می‌شود، پس شناسه‌اش در آن لحظه وجود ندارد.
  ⚠️ **یک قاعده با یک استثنا، نه فهرستی از هفت `kind`.** آن فهرست
  شمرده شد و نگهبان را دقیقاً جایی می‌گذاشت که خطر **نیست**:
  `defective` و `lost` و `correction` مقدارهای **مرده**اند و هیچ مسیری
  نمی‌سازدشان (`'defective'` در کد `warehouse.kind` است، نه
  `stock_movement.kind`). با فهرست هفت‌تایی، اولین مسیرِ «ضایعات» که
  فردا نوشته شود از سوراخ رد می‌شد.
  ⚠️ **دو لایه، و هیچ‌کدام تزئین نیست.** Trigger پیام **فارسی** و
  `P0001` می‌دهد — `errors.ts` فقط `P0001` را به ۴۰۹ ترجمه می‌کند و یک
  `CHECK` خام (`23514`) به کاربر «خرابی سرور» نشان می‌داد. و `CHECK`
  می‌ماند چون Trigger را می‌شود `DISABLE` کرد و `CHECK` سطرهای **موجود**
  را هم اعتبارسنجی می‌کند.
  ⚠️ بند ۶ `db/test/movement-ref.sql` Trigger را خاموش می‌کند و ادعا
  می‌کند `CHECK` هنوز می‌گیرد. بی آن، `CHECK` هرگز سنجیده نمی‌شد چون
  Trigger همیشه زودتر شلیک می‌کند — و یک `DROP CONSTRAINT` سهوی بی‌صدا
  از همهٔ تست‌ها رد می‌شد.
  ⚠️ و بند ۳ همان پرونده **کنترل منفی** است: `opening` بی‌مرجع باید
  **قبول** شود. قیدِ بیش از حد سخت‌گیر، مهاجرت دادهٔ واقعی را می‌شکست —
  شکستی که فقط روز واردات دیده می‌شد، نه در CI.
- **سند نهایی حذف نمی‌شود.**
- **Snapshot اجباری:** `invoice_line` باید `unit_price`، `discount_amount`
  و `unit_cost` را در لحظه فروش ذخیره کند. سود هرگز از قیمت جاری خوانده
  نمی‌شود.
- **قیمت دستی روی فاکتور مجوزدار است و فاکتور یک قیمت نشان می‌دهد.**
  قیمت تازه در `unit_price` می‌نشیند و قیمت فهرست در `list_price` —
  نه به‌شکل تخفیف، چون آن‌وقت دو قیمت روی فاکتور می‌خورد. سقف از
  **کاهش کل** سنجیده می‌شود (تخفیف + تفاوت قیمت)، وگرنه قیمت دستی
  راهِ دور زدن سقف تخفیف بود.
- **کاهش قیمت بالاتر از `discount.require_reason_above_percent` بدون
  دلیل ثبت نمی‌شود.** یک Trigger روی `invoice_line` اجبارش می‌کند، نه
  لایه API.
- **قیمت فهرست سطر یک بار Snapshot می‌شود، نه هر بار.**
  `sales.set_line_price()` قیمت سطری را که در سبد است عوض می‌کند و
  `list_price` موجود را دست نمی‌زند. اگر هر تغییر، قیمت جاری را
  «فهرست» می‌گرفت، دو تغییر پیاپی سقف کاهش را دور می‌زد. برگشت به
  قیمت فهرست، Snapshot و دلیل را پاک می‌کند.
- **قیمت قبلی هرگز `UPDATE` یا `DELETE` نمی‌شود.** تغییر قیمت یعنی
  بستن سطر باز با `valid_to` و درج سطر تازه — تنها از
  `catalog.set_price()`. یک Trigger روی `catalog.price` هر چیز دیگری
  را رد می‌کند و یک ایندکس یکتای جزئی بیش از یک قیمت باز برای هر
  (تنوع، فهرست قیمت) را ممکن نمی‌گذارد. `invoice.currentPrice` قیمت را
  **در لحظه `occurred_at`** می‌خواند، پس یک فاکتور یک قیمت دارد حتی
  اگر وسط کار حراج بخورد.
  ⚠️ مرز از `clock_timestamp()` می‌آید نه `now()`: `now()` زمان شروع
  تراکنش است و دو تغییر قیمت در یک تراکنش قید `valid_to > valid_from`
  را می‌شکستند.
- **رنگ و سایز تنوع فقط پیش از اولین حرکت انبار و اولین فروش عوض
  می‌شوند.** بعد از آن روی برچسب چاپ‌شده و در Snapshot فاکتور
  نشسته‌اند. `catalog.amend_variation()` خودش این را اجبار می‌کند.
- **کالا و تنوع حذف نمی‌شوند، بایگانی می‌شوند.** فاکتور پارسال به همان
  سطر ارجاع می‌دهد.
- **مرجوعی با بهای همان فروش برمی‌گردد**، نه میانگین جاری انبار.
- **مهلت مرجوعی به ساعت است، نه روز** (`return.window_hours`، پیش‌فرض
  ۴۸). گرد کردن روز نمی‌تواند ۴۸ ساعت را بیان کند: با «۲ روز»، فاکتور
  ۷۱ ساعته هم داخل مهلت شمرده می‌شد.
- **روش قیمت تمام‌شده داده است، نه کد.** `costing.method` **سه** مقدار
  دارد و هر سه پیاده‌اند: `last_purchase` (**پیش‌فرض**، تصمیم مالک)،
  `moving_weighted_average`، و `fifo` (مهاجرت ۰۲۰).
  در `last_purchase` هر رسید خرید کل موجودی همان کالا را به نرخ خودش
  ارزیابی می‌کند و تفاوت، سطر تعدیل حساب ۵۱۰۲ می‌گیرد — وگرنه دارایی
  بدون طرف حساب زیاد می‌شود. **جمع سود با هر سه روش یکی است؛ فقط زمان
  شناسایی فرق می‌کند.** جزئیات در ADR-006.
  ⚠️ این بند تا شهریور ۱۴۰۵ «دو مقدار» می‌گفت، ماه‌ها پس از آنکه مهاجرت
  ۰۲۰ روش سوم را آورد. `README.md` درست بود و همین‌جا عقب مانده بود —
  یعنی هر کسی که اول اینجا را می‌خواند، گمان می‌کرد FIFO ساخته نشده.
  ⚠️ استاندارد حسابداری شماره ۸ ایران میانگین موزون، FIFO و شناسایی
  ویژه را می‌پذیرد؛ `last_purchase` در آن فهرست نیست و پیش از
  اظهارنامه باید با حسابدار تأیید شود. **دو تا از سه گزینه در آن فهرست
  هستند**، پس تغییرش یک `UPDATE` است نه یک پروژه.
- **ادعای «جمع سود با هر سه روش یکی است» اندازه‌گیری می‌شود، نه حساب.**
  `db/test/costing-property.sql` همان سناریو را **واقعاً سه بار** اجرا
  می‌کند — یک بار با هر روش — کالا را تا آخر می‌فروشد، دوره ثبت را
  می‌بندد، و سود را از **دفتر** می‌خواند (درآمد و تخفیف و هزینه:
  `credit − debit`)، نه با حساب دستی. ۱۵۰ سناریوی تصادفی و بازتولیدپذیر.
  روش‌ها در یک آرایه‌اند، پس روش چهارم یک عضو است نه یک `IF` تازه.
  ⚠️ بند ۲ آن پرونده **کنترل حساسیت** است و حذف نمی‌شود: نیمی از موجودی
  را نمی‌فروشد و ادعا می‌کند روش‌ها **باید فرق کنند**. آزمونِ برابری که
  نتواند نابرابری را ببیند، سبزِ بی‌معناست — و همین بند، نیمهٔ دوم ادعا
  («فقط زمان شناسایی فرق می‌کند») را هم می‌سنجد.
  ⚠️ ولی «هر سه دوبه‌دو فرق کنند» ادعای **غلطی** است و اندازه‌گیری شد: در
  ۶ سناریو از ۱۲، `last_purchase` و `fifo` به یک عدد رسیدند. کنترل
  حساسیت به‌جایش فرقِ **هر روش با میانگین موزون** را می‌سنجد — که در هر
  ۱۲ سناریو برقرار بود و FIFO را واقعاً وارد کنترل می‌کند.
  ⚠️ در بند ۲ دست‌کم **دو** رسید با نرخ متفاوت لازم است: با یک نرخ،
  تجدید ارزیابی‌ای وجود ندارد و روش‌ها عمداً یکی می‌شوند.
- **تستی که عددش به `costing.method` وابسته است، خودش روش را ست
  می‌کند.** `golden_path` و `edge_cases` با میانگین موزون نوشته
  شده‌اند و صریح انتخابش می‌کنند؛ تکیه به پیش‌فرض یعنی یک `UPDATE` در
  تنظیمات، نصف تست‌ها را قرمز کند.
- **تجدید ارزیابی تنها حرکت با تعداد صفر است.** ارزش عوض می‌شود، تعداد
  نه. قید `stock_movement` این را صریح می‌گوید، پس `balance_check`
  بدون تغییر کار می‌کند.
- **هیچ Retry خودکاری روی پرداخت نامشخص.** فقط صف بررسی انسانی.
- **هر فاکتور نهایی‌شده به یک «دوره ثبت» می‌چسبد.** فاکتور صندوق به دوره
  شیفت، فاکتور آنلاین به دوره (شعبه، کانال، روز). سند فروش و COGS در
  `sales.post_batch()` زده می‌شود، نه در `finalize_invoice`. اگر
  `sales.unposted_revenue` خالی نباشد، درآمدی به دفتر نرفته است.
- **بازپرداخت از پول واقعاً دریافت‌شده بیشتر نمی‌شود**، و مازادِ ارزش
  کالای برگشتی اول بدهی همان فاکتور را صفر می‌کند، بعد اعتبار مشتری.
- **نشست با توکن مات است، نه JWT.** فقط SHA-256 توکن در
  `identity.session` می‌نشیند. مجوز فقط از `identity.can()` که
  `permission_rule` را می‌خواند — **هیچ شرط دسترسی در کد نیست**.
- **تأیید دستگاه مسیر محصول دارد، نه فقط تابع.** `device.manage` در
  Seed فقط مدیر داردش و در `auth.pin_forbidden_operations` هم هست —
  نشست بازشده با PIN نمی‌تواند دستگاه تأیید کند. راز دستگاه از هیچ
  پاسخی بیرون نمی‌رود؛ `identity.device_overview` اصلاً ستونش را ندارد.
- **PIN عامل دوم نیست مگر دستگاه اثباتش کند.** `fingerprint` یک شناسه
  است نه یک راز؛ دستگاه باید راز ثبت‌نام (`device.secret_hash`) هم
  ارائه کند. راز فقط در **اولین ورود کامل پس از تأیید مدیر** صادر
  می‌شود و در کوکی HttpOnly می‌نشیند.
- **نشستِ باز‌شده با PIN ارتقایافته نیست.** `session.pin_unlocked` روشن
  می‌ماند و `identity.can()` عملیات `auth.pin_forbidden_operations` را
  می‌بندد. تنها راه ارتقا `identity.reauth_session()` است — یعنی رمز
  کامل. `viaPin` همیشه از **نشست** خوانده می‌شود، نه از فراخوان.
- **`platform.set_actor()` پیش از هر عملیات حساس اجباری است.** توابع
  مالی خودشان `platform.audit()` می‌زنند و بدون کاربر عامل خطا می‌دهند.
- **هر سطر دریافتنی، امتیاز و بدهی تأمین‌کننده `party_id` دارد.** بدون آن
  گردش حساب اشخاص از دفتر ساختنی نیست. سند تجمیعی می‌تواند چند سطر
  هم‌نام داشته باشد — یکتایی روی قاعده است، نه روی سطر سند.
- **نگاشت حساب فقط در `ledger.posting_rule` است.** تراکنش فقط وقتی
  می‌تواند حساب را تعیین کند که `allow_account_override` آن قاعده روشن
  باشد. درآمد، تخفیف، مالیات، بهای تمام‌شده و موجودی کالا هرگز روشن
  نمی‌شوند.
- **و عوض‌کردنش فقط از `ledger.set_posting_rule()` است** — مهاجرت ۰۵۳،
  با صفحهٔ خودش (تنظیمات → **نگاشت حساب**). تا آن مهاجرت، `ledger.account`
  مسیر API و صفحه داشت ولی نگاشتش نداشت: حسابدار می‌توانست حساب ۴۱۰۲ را
  بسازد و نمی‌توانست بگوید «تخفیف را به آن بزن». آن یک `UPDATE` دستی در
  psql بود، بی ردّ حسابرسی و بی نگهبان.
  ⚠️ **نوع حساب باید یکی بماند.** «فروش کالا» از ۴۱۰۱ به ۴۲۰۲ می‌رود (هر
  دو `revenue`) ولی به ۱۳۰۱ نه (`asset`). بی این نگهبان، نگاشتِ درآمد به
  یک حساب دارایی ترازنامه را **بی‌صدا** غلط می‌کرد: سند متوازن می‌ماند و
  هیچ ثابتی قرمز نمی‌شد (بند ۵۵ الحاقیه، توازن ≠ صحت معنایی).
  ⚠️ `event_type`/`leg`/`side` از آن مسیر عوض **نمی‌شوند**: قرارداد کدند
  و `sales.post_batch()` مؤلفه را به نام می‌خواند. `is_active` و
  `allow_account_override` هم نه.
  ⚠️ و **تغییر نگاشت تاریخ را بازنویسی نمی‌کند** — بند ۷ تست همین را
  ادعا می‌کند. مجوزش `ledger.mapping` است، جدا از `settings.security`،
  تا مالک بتواند نگاشت را به حسابدار بدهد بی‌آنکه کلیدهای امنیتی
  تنظیمات را هم داده باشد. در Seed فقط مدیر داردش.
- **نامهٔ مرده فقط از `platform.requeue_dead_letter()` زنده می‌شود** —
  مهاجرت ۰۵۴، معیار پذیرش ۱۱. `UPDATE status='pending'` دستی دو مشکل
  داشت: ردّ حسابرسی نداشت، و **بی‌اثر بود** — `attempts` روی سقف می‌ماند
  و اولین شکستِ بعدی همان لحظه دوباره می‌کشتش. یعنی اجرای مجددی که یک
  بار هم تلاش نمی‌کرد.
  ⚠️ صفرکردن `attempts` بخشِ اصلی این تابع است، نه یک جزئیات. و
  `last_error` پاک می‌شود و در `audit_log` می‌نشیند — پیامی که بعداً
  موفق شد نباید تا ابد یک خطای قدیمی روی خودش داشته باشد.
  ⚠️ یکی‌یکی، نه دسته‌ای: هر پیام دلیل مرگ خودش را دارد.
- **موجودی و قیمت لحظه‌ای به سایت می‌روند** — مهاجرت ۰۵۷، ADR-007.
  نقطهٔ اتصال `inventory.apply_movement()` و `catalog.set_price()` است،
  نه مسیرهای فروش و خرید: چون «موجودی فقط از apply_movement تغییر
  می‌کند»، هر مسیری خودبه‌خود پوشش می‌گیرد و هیچ‌کدام جا نمی‌ماند.
  ⚠️ **مقدار مطلق، نه تفاضل.** تحویل Outbox «حداقل یک بار» است؛ با
  «−۱» یک تحویل تکراری یا گم‌شده موجودی سایت را **برای همیشه** خراب
  می‌کرد. با «الان ۷ است»، تکرار بی‌ضرر است.
  ⚠️ **تجمیع با ایندکس یکتای جزئی:** برای هر (موضوع، تنوع) حداکثر یک
  سطر معلق. ۵۰ فروش پشت سر هم یک درخواست می‌سازد، نه ۵۰ تا — و صف
  به اندازهٔ «تنوع‌های تغییرکرده» رشد می‌کند نه «حرکت‌ها». سطری که
  `sending` شده بازنویسی نمی‌شود؛ آن پیام **بعدی** می‌سازد.
  ⚠️ **عدد سایت باید با خوراک ۱۵ دقیقه‌ای یکی باشد** (`on_hand −
  reserved` روی `web.stock_warehouse`). دو تعریف یعنی تور ایمنیِ تطبیق
  هر بار واگرایی **کاذب** بدهد و خیلی زود کسی نگاهش نکند.
  ⚠️ `version` از دنبالهٔ `platform.web_push_version` می‌آید، نه از
  شناسهٔ سطر — آن‌ها `uuid`اند نه شمارنده (ADR-007 این را غلط گفته بود).
  ⚠️ کلید امضا در `WEB_PUSH_SECRET` و `LMC_PUSH_SECRET` است، نه در
  `platform.setting`. نشانی سایت یک **تصمیم** است و در جدول؛ کلید یک
  **راز** است و در محیط.
  ⚠️ `platform.web_push_health` پیکربندی ناسازگار را می‌بیند: Push روشن
  و انبار تعیین‌نشده یعنی هیچ پیامی ساخته نشود و **همه‌چیز سالم به‌نظر
  برسد**.
- **میان‌بر «پیامک خاموش است» فقط برای موضوع‌های پیامکی است.** حلقهٔ
  Worker هر پیامی را با خاموشیِ پیامک بی‌صدا `sent` می‌کرد؛ از وقتی
  `health.alert` هم Webhook می‌فرستد، یعنی هشدار سلامت با پیامکِ خاموش
  **بی‌صدا بسته می‌شد**. `SMS_ONLY_TOPICS` این را بست.
- **هشت زنگ خطر یک تعریف دارند: `platform.health_alerts()`** — مهاجرت
  ۰۵۵. سه مصرف‌کننده: صفحهٔ «سلامت سیستم»، `platform.enqueue_health_alerts()`
  و `ops/deploy.sh status`. هشت کوئری در سه جا یعنی هر زنگ تازه دو جا را
  عقب می‌گذارد — و **بی‌صدا**، چون نبودِ یک هشدار شبیه «همه‌چیز خوب است»
  به نظر می‌رسد.
  ⚠️ **«درآمد ثبت‌نشده» در طول روز هشدار نیست.** دوره ثبت امروز باز
  می‌ماند و کار شبانه دیروز را می‌بندد، پس این زنگ فقط روزهای **گذشته**
  را می‌شمارد. بی این تفکیک، هشدار هر روز ظهر می‌رفت و روزی که واقعاً
  چیزی گیر می‌کرد همان پیام نادیده گرفته می‌شد.
  ⚠️ **فهرست گزینه‌های `notify.health_alert_codes` باید دقیقاً برابر
  کدهای آن تابع باشد** — و یک تست هر دو جهت را می‌سنجد: کدِ بی‌گزینه
  قابل خاموش‌کردن نیست، و گزینهٔ بی‌کد کاربر را منتظر هشداری می‌گذارد که
  هرگز نمی‌آید.
  ⚠️ Idempotency روی کلید (کد زنگ، روز کاری) در **payload** است، مثل
  هشدار چک — حلقهٔ Worker ساعتی است.
- **`makeWebhookSender` نوشته و تست شده بود و هیچ‌کس صدایش نمی‌زد.**
  `notify.webhook_enabled` در صفحهٔ تنظیمات وعدهٔ «ارسال لینک فاکتور»
  می‌داد و روشن‌کردنش **هیچ کاری نمی‌کرد** — همان کلاسِ
  `backup.restore_drill_days`. حالا `loop.ts` فرستنده را می‌سازد و هم
  فاکتور و هم هشدار سلامت از آن می‌روند. توکنش از `NOTIFY_WEBHOOK_TOKEN`
  می‌آید نه از `platform.setting`، چون راز است.
  ⚠️ در Handler فاکتور، **پیامک اول و Webhook بعد**: اگر Webhook اول بود
  و ۵xx می‌داد، پیامکِ مشتری هم تا تلاش بعدی معلق می‌ماند.
- **هر حرکت نقدِ غیرفروشی باید `treasury.transaction` با `shift_id` باشد**،
  وگرنه شمارش صندوق مغایرت کاذب می‌دهد. دروازه‌اش در
  `http/treasury-routes.ts` است، نه دیتابیس — دیتابیس نمی‌داند کدام
  کاربر کدام شیفت را باز دارد. شیفت از **شعبه** گرفته می‌شود نه از
  کاربر عامل: مدیر برای کرایه پیک از کشوی صندوق‌دار برمی‌دارد و خودش
  شیفتی ندارد. دو کشوی باز یعنی ابهام و ۴۲۲، نه حدس.
- **کارمزد و دوره تسویه فقط از `treasury.set_settlement_terms()`.**
  در `platform.setting` نیستند و نباید بروند: از قبل در
  `treasury.account` هستند و همان‌جا خوانده می‌شوند، و **به‌ازای هر
  پایانه** معنا دارند — کارت‌خوان فروشگاه و درگاه سایت یک قرارداد
  ندارند. فهرست کلیدهای صفحه تنظیمات یک سطر **فقط‌خواندنی** به‌ازای هر
  پایانه نشان می‌دهد که به زبانه «پایانه‌ها» می‌برد: نبودنش آنجا، کسی
  را که دنبال «کارمزد» می‌گردد به این نتیجه می‌رساند که از تنظیمات
  عوض نمی‌شود و سراغ کد برود. کارمزد صفر هشدار می‌گیرد، چون از Seed
  آمده نه از قرارداد.
- **وضعیت چک فقط از `treasury.post_cheque_event()` حرکت می‌کند.** ستون
  `status` یک Projection از زنجیره رویداد است؛ `UPDATE` مستقیم رویش با
  `CONSTRAINT TRIGGER` معوق رد می‌شود. چک تا وصول نشود پول نیست و هرگز
  مستقیم به صندوق یا بانک نمی‌رود.
- **تنظیمات فقط از `platform.set_setting()` عوض می‌شوند.** مقدار یک
  تنظیم مالی خودش داده مالی است: نوع، گزینه و بازه اعتبارسنجی می‌شوند،
  تغییر با مقدار پیش و پس در `audit_log` می‌نشیند، و کلید تازه از این
  مسیر **ساخته نمی‌شود** (فقط مهاجرت و seed). تنظیمی که
  `is_editable = false` دارد — `payment.unknown_auto_retry` و
  `currency.base` — از هیچ مسیری باز نمی‌شود؛ آن یک قاعده است، نه یک
  پیش‌فرض.

## تصمیم‌های باز = داده، نه کد

کدینگ حساب، نرخ مالیات، سقف تخفیف و روش هزینه‌یابی هنوز تصویب نشده‌اند.
همه در جدول‌اند تا تغییرشان `UPDATE` باشد نه Deploy:

`platform.setting` · `ledger.account` · `ledger.posting_rule` ·
`identity.permission_rule`

**هیچ‌کدام را در کد hardcode نکن.** اگر منطقی نیاز به کد حساب دارد، از
`posting_rule` بخوان.

`platform.setting` حالا فراداده هم دارد — نوع، برچسب فارسی، گزینه‌ها،
بازه مجاز، گروه، و **عملیات مجوزی که تغییرش را می‌سنجد**. صفحه
`apps/web` از همین فراداده ساخته می‌شود، پس تنظیم تازه بدون یک خط کد
UI دیده می‌شود. اگر وسوسه شدی فهرست کلیدها را در React بنویسی، یعنی
یک ستون فراداده در `db/seed/030_settings.sql` کم است.

## دستورها

```bash
docker compose up -d db                      # فقط پستگرس
export DATABASE_URL='postgres://labelmod:<رمز>@localhost:5432/labelmod'

ops/db.sh migrate     # فقط مهاجرت‌های اجرانشده (دفتر: public.schema_migration)
ops/db.sh baseline 031 # یک بار روی دیتابیسی که پیش از دفتر بالا آمده
ops/db.sh seed        # کدینگ حساب، قواعد ثبت، تنظیمات، داده مرجع
ops/db.sh test        # تست‌های مالی — روی دیتابیس یک‌بارمصرف
ops/db.sh reset       # بازسازی کامل
ops/db.sh backup      # دامپ رمزنشده — باید خارج از سرور کپی شود
ops/restore-drill.sh  # بکاپ را واقعاً برمی‌گرداند و ادعاها را می‌راند
APP_PASSWORD='…' ops/db-roles.sh  # نقش برنامه بدون حق نوشتن مستقیم (بند ۳ SECURITY.md)

ops/install-hooks.sh  # یک بار روی هر کلون
ops/close-due-days.sh # بستن شبانه دوره فروش سایت — برای cron

# استقرار تولیدی — راهنمای کامل در docs/DEPLOYMENT.md
ops/deploy.sh up      # Build و اجرای docker-compose.prod.yml
ops/deploy.sh migrate # مهاجرت داخل ظرف، بدون پورت باز پستگرس
ops/deploy.sh seed
ops/deploy.sh user --username ali --name '…' --role admin --branch MAIN
ops/deploy.sh api-client --name 'سایت' --role web --branch MAIN  # کلید ووکامرس
ops/deploy.sh status  # درآمد ثبت‌نشده + پیام‌های نرفته + چک سررسیدشده

corepack enable       # یک بار — نسخه pnpm از packageManager خوانده می‌شود
pnpm install
pnpm check            # lint + typecheck + test
pnpm lint             # ESLint — از ریشه، روی کل مخزن
pnpm lint:fix
pnpm --filter @labelmod/api dev
```

`pnpm test` تست یکپارچه را روی یک **دیتابیس یک‌بارمصرف** اجرا می‌کند که
خودش می‌سازد و می‌اندازد. اگر `DATABASE_URL` نباشد، آن تست‌ها skip
می‌شوند و پیامش را می‌بینید — سبز وانمود نمی‌کنند.

`ops/db.sh test` برای هر اجرا یک دیتابیس تازه می‌سازد و پاک می‌کند.
دلیلش: تست همزمانی به دو نشست مجزا نیاز دارد و نمی‌تواند Rollback شود؛
داده Commit‌شده‌اش ادعاهای سناریوی طلایی را در اجرای بعدی می‌شکست.

## گلوگاه‌ها

- **پیش از هر تغییر در `db/migrations/0[01][0-9]_*.sql`، `ops/db.sh test` را
  اجرا کن و بعد از تغییر دوباره.** ۱۲۸۴ ادعای SQL به‌علاوه ۸۸۴ تست Node و ۱۲۴ ادعای PHP افزونه باید پاس شوند.
- `db/test/purchase-receipt.sql` شماره‌گذاری و تخصیص هزینه رسید خرید را،
  `db/test/purchase-return.sql` برگشت از خرید را،
  `db/test/purchase-order.sql` سفارش خرید را، و
  `db/test/stock-count.sql` انبارگردانی را —
- **«یک روز کامل فروشگاه» تنها تستی است که تعامل ماژول‌ها را می‌سنجد.**
  `apps/api/test/full-store-day.integration.test.ts` هجده مرحله را از
  ۰۸:۰۰ تا ۲۳:۵۹ **از راه HTTP** می‌راند — نه با صدا زدن توابع SQL، چون
  دروازه‌های واقعی (سقف کاهش قیمت، کشوی صندوق، دامنه شعبه) در لایه API
  هستند. بعد `db/reconcile/full-day.sql` دوازده تطبیق مستقل می‌راند، و
  بعد همان دیتابیس **بکاپ و Restore** می‌شود و هر دوازده تطبیق دوباره.
  ⚠️ آن پرونده عمداً در `db/test/` **نیست**: `ops/db.sh test` هر
  `db/test/*.sql` را روی دیتابیس **خالی** اجرا می‌کند و یک تطبیقِ
  فقط‌خواندنی آنجا بی‌معنا سبز می‌شد. بند ۰ خودش هم ضد‌پوچی دارد و اگر
  دادهٔ روز نباشد پیش از هر تطبیقی خطا می‌دهد.
  ⚠️ و `RAISE NOTICE` روی **stderr** psql می‌نشیند نه stdout — ادعایی که
  فقط stdout را بگیرد، متن تطبیق را هرگز نمی‌بیند.
- `db/test/regressions.sql` هشت باگ تأییدشده را قفل می‌کند،
  `db/test/treasury.sql` مسیرهای پول را، `db/test/cheques.sql` ماشین
  وضعیت چک را، `db/test/identity.sql` نشست، قفل ورود و مجوز را و
  `db/test/settings.sql` اعتبارسنجی و ردّ حسابرسی تنظیمات را و
  `db/test/costing.sql` هر دو روش قیمت تمام‌شده را و
  `db/test/price-override.sql` قیمت دستی و اجبار ثبت دلیل را و
  `db/test/transfer.sql` انتقال بین انبارها و ثابت‌بودن ارزش را و
  `db/test/reports.sql` هشت گزارش را در برابر همان منبعی که باید از آن
  بیایند (خلاصه روز، `stock_balance`، توازن دفتر) و
  `db/test/restore-drill.sql` زنگ تمرین بازیابی را و
  `db/test/auto-close.sql` بستن خودکار دوره کانال را و
  `db/test/product-pricing.sql` چرخه حیات کالا و تاریخچه قیمت را و
  `db/test/device-admin.sql` تأیید و ابطال دستگاه و ابطال دسترسی را و
  `db/test/api-client.sql` کلید ماشینی و مجوز نقش سایت را و
  `db/test/outbox.sql` اجاره صف، Backoff و نامه مرده را و
  `db/test/outbox-requeue.sql` زنده‌کردن نامهٔ مرده را و
  `db/test/posting-rule.sql` تغییر نگاشت حساب را و
  `db/test/health-alerts.sql` هشت زنگ خطر و تفکیک «امروز» از «دیروز» را و
  `db/test/write-gate.sql` این را که هیچ نویسندهٔ جدول قفل‌شده‌ای بدون
  `SECURITY DEFINER` نمانده باشد **و هیچ تابع DEFINERی `EXECUTE` برای
  `PUBLIC` نداشته باشد** (با کنترل مثبتی که یک تابع بازِ موقت می‌سازد) و
  `db/test/movement-ref.sql` اجبار سند مرجع روی حرکت انبار را (با کنترل
  مثبت و **منفی** و ادعای مستقل روی هر دو لایه) و
  `db/test/db-roles.sh` سیزده حمله با **نقش برنامه** به‌علاوهٔ دو حمله با
  نقشی که فقط `USAGE` روی یک اسکیما دارد، و چهار دروازه‌ای که باید باز
  بمانند (`apply_movement`، `revalue_to_cost`، `set_price`، و پرشدن صف
  Push از داخل `apply_movement`) را و
  `db/test/restore-drill-guard.sh` این را که تمرین بازیابی در نبود بکاپ
  **حرف بزند** (نه اینکه بی‌صدا با کد ۱ بمیرد) را. اگر یکی قرمز شد، یک اصلاح برگشته — نه اینکه تست سخت‌گیر
  است.
- **README همراه هر تصمیمِ بسته‌شده به‌روز می‌شود.** جدول «تصمیم‌های
  باز» و فهرست «هنوز ساخته نشده» در `README.md` همان چیزی است که آدم
  اول می‌بیند؛ اگر عقب بماند، **غلط** می‌شود نه فقط قدیمی — و یک بار
  همین اتفاق افتاد (مهلت مرجوعی ۷ روز، روش میانگین موزون، و «API هنوز
  ساخته نشده» ماه‌ها بعد از ساخته‌شدنشان آنجا مانده بودند). هر
  مهاجرتی که یک تنظیم را عوض کند یا قابلیتی اضافه کند، README را هم
  عوض می‌کند.
- **ردیف تکراری در Seed مجوزها بی‌صدا رد می‌شود.**
  `ON CONFLICT (role_code, operation) DO NOTHING` دو ردیف یکسان در
  همان VALUES را بدون خطا نادیده می‌گیرد — پس می‌شود یک قاعده را با
  مقدار **متفاوت** دوباره نوشت و اولی برنده شود. یک بار همین شد.
  `apps/api/test/source-hygiene.test.ts` حالا این کلاس را می‌بندد.
- **TOTP بدون کتابخانه است و با بردارهای خودِ RFC 6238 سنجیده
  می‌شود.** تستی که کد را با همان تابع تولید کند فقط سازگاری تابع با
  خودش را ثابت می‌کند، نه سازگاری با Google Authenticator. بردار
  `t=20000000000` عمداً هست: پیاده‌سازی‌هایی که شمارنده را با `<<`
  می‌نویسند دقیقاً همان‌جا می‌شکنند.
- **راز TOTP تأییدنشده، روی `app_user` نمی‌نشیند.** تا کاربر یک کد
  درست ندهد، راز در `identity.totp_enrollment` می‌ماند. اگر یکی
  بودند، کسی که وسط ثبت‌نام رها می‌کرد فردا پشت کدی قفل می‌شد که
  هرگز اسکن نکرده.
- **مرحله دوم ورود، نشست نمی‌سازد.** رمز درست یعنی یک بلیت
  کوتاه‌عمر در `identity.pending_login` — نه کوکی نشست، نه راز
  ثبت‌نام دستگاه. بند ۱ SECURITY.md می‌گوید راز دستگاه فقط پس از
  ورود **کامل** صادر شود.
- **کد ثبت‌نام غلط ۴۲۲ می‌دهد، نه ۴۰۱.** کاربر وارد شده و فقط اشتباه
  تایپ کرده؛ ۴۰۱ یعنی کلاینت او را از صفحه بیرون بیندازد. `bad_code`
  (مرحله دوم ورود) همچنان ۴۰۱ است چون آنجا واقعاً نشستی نیست.
- **راز از هیچ پاسخی بیرون نمی‌رود — و این با «حذفش می‌کنیم» تضمین
  نمی‌شود.** `password_hash`، `pin_hash` و `totp_secret` اصلاً در
  `select` مسیر پرسنل نیستند، مثل `identity.device_overview` که ستون
  راز را ندارد. پاسخ فقط `hasPin` و `hasTotp` می‌دهد.
- **رمز را سرور می‌سازد و یک بار برمی‌گرداند.** گرفتن رمز از فرم یعنی
  همان رمزی که مدیر برای همه انتخاب می‌کند — سطر اول فهرست کنترل بند
  ۸ SECURITY.md. غیرفعال‌کردن کاربر و تغییر رمز، هر دو **همان لحظه**
  همه نشست‌هایش را می‌بندند.
- **کسی نمی‌تواند خودش را بیرون بیندازد.** مدیری که حساب خودش را
  غیرفعال کند دیگر نمی‌تواند برش گرداند و تنها راه SSH به سرور است.
  این یک قاعده **ایمنی** است، نه دسترسی.
- **نقش «همه شعبه‌ها» را فقط کسی می‌دهد که خودش دارد.** وگرنه کاربر
  شعبه‌ای می‌توانست کاربری با دسترسی کامل بسازد و از راه او داده
  شعبه دیگر را ببیند.
- **شماره موبایل تکراری مشتری دوم نمی‌سازد.** نرمال‌سازی در دیتابیس
  است (`sales.normalize_mobile`)، نه در TypeScript: دو تعریف یعنی
  مشتری‌ای که یک بار آنلاین و یک بار حضوری خرید کند دو حساب داشته
  باشد و مانده‌اش بینشان گم شود.
- **بایت کنترلی خام در سورس ننویس — Escape بنویس.** یک NUL خام کافی
  است تا گیت کل فایل را باینری بداند؛ آن‌وقت `git diff` می‌گوید
  «Binary files differ»، `git grep -I` ردش می‌کند پس اسکن راز pre-push
  رویش اجرا نمی‌شود، و هیچ‌کدام خطا نمی‌دهند. یک بار همین اتفاق افتاد و
  ۳۴ مسیر خرید ماه‌ها بدون Diff Merge شدند.
  `apps/api/test/source-hygiene.test.ts` و بند ۳ هوک pre-push حالا
  کلاسِ این باگ را می‌بندند.
- **`::date` روی یک ستون `_at` ممنوع است — `platform.business_date()` بنویس.**
  `x::date` روی `timestamptz` تاریخ را از منطقه زمانی **سرور** می‌گیرد و
  سرور تولید UTC است. مهاجرت ۰۱۴ این کلاس را بست، ۰۲۳ دوباره، و هر
  مهاجرتِ بعدی دوباره نوشتش — تا ۰۳۹ ده تابع مالی را پیدا کرد که سند
  را با تاریخ سرور می‌زدند. اثرش فقط بین ۰۰:۰۰ تا ۰۳:۳۰ بامداد به وقت
  تهران دیده می‌شود، پس تست‌ها ۲۰٫۵ ساعت از شبانه‌روز سبز بودند. حالا
  `db/test/business-date.sql` هر `_at::date` و هر `now()::date` را در
  تعریف توابع و نماها رد می‌کند. **بازه تاریخی روی یک ستون زمان‌دار هم
  همین است:** مرزش از `platform.business_day_start()` می‌آید، نه از
  `p_from::timestamptz` (مهاجرت ۰۳۸).

- **`pnpm lint` دیگر no-op نیست — و یک تست جلوی برگشتنش را می‌گیرد.**
  اسکریپت ریشه `pnpm -r --if-present lint` بود و `--if-present` روی
  پکیجی که اسکریپت ندارد **بی‌صدا صفر برمی‌گرداند**؛ یعنی CI ماه‌ها سبز
  می‌شد بی‌آنکه یک خط Lint شده باشد. حالا `eslint .` از ریشه اجرا
  می‌شود و `source-hygiene.test.ts` می‌سنجد که اسکریپت واقعاً ESLint را
  صدا بزند و CI اجرایش کند.
  ⚠️ **قاعده‌های تایپ‌آگاه روشن نیستند:** هیچ نسخه‌ای از
  `typescript-eslint` هنوز TypeScript 7 را پشتیبانی نمی‌کند، پس یک
  نسخه TS 6 فقط برای Lint در `pnpm.overrides` پین شده — همان راهی که
  خودِ TypeScript مستند کرده. هرگاه پشتیبانی آمد، Override برداشته و
  `projectService` روشن می‌شود.
- **زیرمجموعه strip-only حالا در Lint اجبار می‌شود.** `enum`،
  `namespace` و parameter property از `tsc` رد می‌شوند ولی Node با
  `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` می‌ایستد — خطایی که فقط در زمان
  اجرا دیده می‌شود. حالا در زمان Lint گرفته می‌شود.
- **نویسه‌های ممنوع ورودی یک تعریف دارند: `apps/api/src/lib/text.ts`.**
  همان الگو در چهار مسیر کپی شده بود؛ یک فیلتر امنیتی با چهار نسخه،
  چهار برابر شانس عقب‌ماندن دارد.

- **زنجیره هش حسابرسی از روز اول بود و **هیچ‌جا بازمحاسبه نمی‌شد**.**
  دو نگاه‌کننده داشت — ادعای CI و بند ۴ `ops/restore-drill.sh` — و هر
  دو فقط `prev_hash = lag(hash)` را می‌سنجیدند، یعنی فقط **پیوند**ها.
  اندازه‌گیری شد: با خاموش‌کردن Trigger و عوض‌کردن `after` یک سطر،
  نگهبان CI ۰ گسست داد، نگهبان drill ۰ گسست، و بازمحاسبهٔ واقعی ۱ سطر
  دست‌کاری‌شده. یعنی زنجیره فقط درج و حذف سطر را می‌گرفت.
  ⚠️ و فرمول نسخه ۱ `before` و `reason` را **نمی‌پوشاند** — دقیقاً دو
  میدانی که یک فرد درون‌سازمانی جعل می‌کند («قبلاً چه بود» و «چرا»).
  رکورد جعلی هر سه نگاه‌کننده **و لنگرِ شبِ بند ۳ SECURITY.md** را رد
  می‌کرد، پس آن وعده برای این دو میدان برقرار نبود.
  مهاجرت ۰۵۱: `platform.audit_hash()` تنها تعریف فرمول است،
  `hash_version` سطرهای تاریخی را نسخه ۱ نگه می‌دارد و تازه‌ها را ۲
  می‌کند، و نمای `platform.audit_check` **پیوند و محتوا** را با هم
  می‌سنجد. سه مصرف‌کننده: CI، `ops/deploy.sh status`، `restore-drill`.
  `db/test/audit-chain.sql` اثبات می‌کند آشکارساز واقعاً می‌گیرد —
  هفت میدان یکی‌یکی، به‌علاوهٔ کنترل مثبت (دفتر سالم = صفر سطر) که
  بی‌آن یک نمای همیشه‌خالی هم «پاس» می‌شد.
- **`party_id` باید به شخصی اشاره کند که وجود دارد.** ثابت CI فقط
  `party_id IS NULL` را می‌سنجید — «داشتن» را، نه «درست بودن» را. و
  `party_id` عمداً FK ندارد چون چندریختی است (مشتری یا تأمین‌کننده).
  نتیجه: `post_entry` یک شناسهٔ ناموجود را می‌پذیرفت و
  `report_party_balances` مانده‌ای با **نام و کد تفصیلی NULL** نشان
  می‌داد — بی مغایرت با ترازنامه، چون سند متوازن بود (بند ۵۵ الحاقیه:
  توازن ≠ صحت معنایی). مهاجرت ۰۵۲ یک Trigger روی `journal_line` گذاشت
  (نه اصلاح `post_entry`: Trigger از هر مسیری می‌گذرد) به‌علاوهٔ نمای
  `ledger.party_check` برای دادهٔ موجود.
  ⚠️ `party_type = 'other'` جدولی ندارد و مستثناست؛ ولی `party_id`
  **بدون** `party_type` رد می‌شود — شناسه‌ای که نوعش را ندانیم در هیچ
  جدولی جست‌وجو نمی‌شود.
  ⚠️ و حذف یک مشتری، سطرهای دفترش را **یتیم** می‌کند: از API ممکن
  نیست (هیچ مسیر حذفی وجود ندارد) و مدل، بایگانی است نه حذف، ولی
  `party_check` حالا می‌بیندش.
- **`ip` و `device` لاگ حسابرسی از «زمینهٔ درخواست» می‌آیند، نه از امضای
  هر متد.** ستون‌ها و `platform.set_actor(uuid, inet, text)` از روز اول
  بودند، ولی ~۷۰ فراخوان `setActor(trx, actorId)` در همهٔ ماژول‌های مالی
  فقط دو آرگومان اول را می‌دادند — پس `session.open` هر دو را داشت و
  `shift.open` و `invoice.finalize` هیچ‌کدام. معیار پذیرش ۱۳ سند برقرار
  نبود. `lib/request-context.ts` یک `AsyncLocalStorage` است که Hook
  فستیفای پرش می‌کند و `setActor` خودش برش می‌دارد، پس آن ۷۰ فراخوان
  دست‌نخورده ماندند.
  ⚠️ **`enterWith` ممنوع، `run` اجباری.** نسخهٔ اول `enterWith` می‌زد و
  هم پس از یک `await` بی‌اثر بود و هم زمینه را **بیرون از درخواست نشت
  می‌داد** — همان کلاسی که `set_actor` با `is_local = true` از آن پرهیز
  می‌کند. Hook به‌شکل Callback است و `done` داخل `run` صدا زده می‌شود.
  ⚠️ بیرون از HTTP (Worker، CLI) هر دو `NULL` می‌مانند و **این درست
  است**؛ یک بند تست عمداً همین را می‌سنجد.
  ⚠️ و دو تعریف `setActor` به یکی رسید (`db/actor.ts` دیگر SQL نمی‌زند).
- **Correlation ID از HTTP تا دفتر حسابرسی و صف پیام می‌رود** — مهاجرت
  ۰۵۶، یافتهٔ FND-018. تا آن مهاجرت `req.id` فقط در لاگ و در پاسخ خطا
  بود، پس از یک **مغایرت مالی** که چند روز بعد پیدا شود راهی به درخواست
  HTTP نبود. حالا از راه GUC می‌رود، نه از راه امضای توابع — پس هیچ
  فراخوان `platform.audit()` عوض نشد.
  ⚠️ پارامتر چهارم **همان `set_actor`** است، نه یک `set_correlation()`
  جدا: مسیری که تابع جدا را صدا نزند، شناسهٔ **درخواست قبلی** را روی
  اتصال Pool‌شده می‌دید.
  ⚠️ و فرمول هش حسابرسی نسخهٔ ۳ شد، وگرنه میدان تازه بیرون از پوشش هش
  می‌ماند و جعلش دیده نمی‌شد (همان اشکال FND-017).
- **اسکریپتی که با `set -euo pipefail` می‌نویسی، پیامش را پشت آن جا
  نگذار.** `ops/restore-drill.sh` پیام «بکاپ پیدا نشد» را داشت و هرگز
  چاپ نمی‌کرد: `find` روی پوشهٔ **ناموجود** کد ۱ می‌دهد، `pipefail` آن
  را به کل لوله می‌برد، و `set -e` اسکریپت را **پیش از** آن پیام
  می‌کشد. خروجی صفر بایت، کد ۱ — یعنی cron شبانه هر شب می‌شکست و کسی
  یک کلمه نمی‌دید.
  ⚠️ پوشهٔ **موجود ولی خالی** بازتولیدش نمی‌کند؛ آنجا `find` موفق است و
  پیام چاپ می‌شود. هر بازتولیدی باید حالت **ناموجود** را هم بسنجد.
  ⚠️ `head -1` هم می‌تواند با SIGPIPE همین اثر را بسازد. `|| true` روی
  خودِ جایگزینی فرمان، هر دو را می‌بندد.
- **سرجمع را با دست ننویس — از خودِ جدول بشمار.** در این مخزن **سه بار**
  یک سرجمع دستی غلط درآمد (۷۴ بند الحاقیه، جدول شدت یافته‌ها، و
  معیارهای پذیرش) و هر سه بار **خودسازگار** به نظر می‌رسید: جمعشان با
  مخرج می‌خواند، پس هیچ‌کس دوباره نمی‌شمرد.
  ⚠️ «پیاده‌سازی‌شده ولی راستی‌آزمایی‌نشده» یک وضعیت **جدا** از «کامل و
  آزموده‌شده» است و یکی نوشتنشان، یک معیارِ **مسدود** را «برقرار»
  می‌شمارد. `source-hygiene.test.ts` حالا `ACCEPTANCE.md` را می‌خواند،
  وضعیت‌ها را می‌شمارد، و با سطر خلاصه **و** `REPORT.md` تطبیق می‌دهد.
- **تمرین بازیابی یک رکورد است، نه یک جمله در سند.** تنظیم
  `backup.restore_drill_days` از روز اول بود و **هیچ‌جا خوانده
  نمی‌شد** — سند وعده هشدار می‌داد و کد هشداری نمی‌ساخت. حالا
  `ops/restore-drill.sh` دامپ را در یک دیتابیس یک‌بارمصرف برمی‌گرداند،
  یازده ادعا رویش می‌راند (از جمله زنجیره حسابرسی **حلقه‌به‌حلقه** و تطبیق
  ارزش موجودی دفتر با انبار) و در
  `platform.restore_drill` ثبت می‌کند. `ops/deploy.sh status` زنگش را
  نشان می‌دهد.
  ⚠️ **تمرین ناموفق زنگ را خاموش نمی‌کند** — وگرنه کافی بود اسکریپت هر
  شب اجرا شود و شکست بخورد تا هیچ‌کس هرگز هشداری نبیند.

- **«راز بیرون نمی‌رود» روی کل سطح API سنجیده می‌شود، نه مسیر‌به‌مسیر.**
  `secret-leak.integration.test.ts` فهرست مسیرها را از **خودِ Fastify**
  می‌خواند، هر `GET` بی‌پارامتر را می‌زند (امروز ۵۵ تا) و پاسخ را دنبال
  کلیدهای ممنوع می‌گردد — پس مسیر تازه‌ای که فردا اضافه شود خودبه‌خود
  پوشش می‌گیرد. یک راز واقعی هم در دیتابیس کاشته می‌شود تا ادعا چیزی
  برای گرفتن داشته باشد.
- **فهرست استثناهای CSRF و مسیرهای عمومی در تست قفل است.** رفتار CSRF
  جای دیگری سنجیده می‌شود، ولی خطر واقعی **اندازه فهرست استثنا**ست:
  مسیری که «چون کار نمی‌کرد» به آن اضافه شود، هیچ تستی را قرمز
  نمی‌کرد. حالا افزودنش باید در دو جا دیده شود.

- **قید معوق تا `COMMIT` بررسی نمی‌شود — و تست `.sql` هرگز به COMMIT
  نمی‌رسد.** هر ۴۴ پروندهٔ `db/test/*.sql` با `ROLLBACK` تمام می‌شوند، پس
  سه قید معوق (`entry_balanced`، `cheque_status_matches_events`،
  `single_open_opening_t`) در آن‌ها **شلیک نمی‌کنند**. ادعاهای موجود سبزند
  چون بررسی **فوری** داخل `post_entry` و `post_cheque_event` زودتر خطا
  می‌دهد — یعنی لایه‌ای که جلوی `INSERT` مستقیمِ دور‌زننده را می‌گیرد،
  سنجیده نمی‌شد. با خنثی‌کردن `assert_entry_balanced` هر ۱۱۲۶ ادعای SQL
  **سبز ماندند**.
  `db/test/deferred.sh` این را بست: از دروازه عبور می‌کند، مستقیم درج
  می‌زند، و `COMMIT` می‌کند.
  ⚠️ **کنترل مثبت اجباری است:** یک درج متوازن باید COMMIT شود. بی‌آن،
  قیدی که همیشه خطا بدهد هم «پاس» می‌شد و کل فروش را می‌شکست.
- **سند افتتاحیه یک سند باز در هر (شعبه، سال) دارد — و این با قفل
  اجبار می‌شود، نه با یک `SELECT`.** `ledger.post_opening_balance()` اول
  `pg_advisory_xact_lock` روی (شعبه، سال) می‌گیرد و بعد سند قبلی را
  می‌خواند. بدون آن قفل، دو فراخوان هم‌زمان هر دو «سند قبلی نیست»
  می‌دیدند و افتتاحیه **دو برابر** می‌شد — اثبات شد، نه حدس.
  ⚠️ **قید معوق این را نمی‌گیرد** و جای قفل را نمی‌تواند بگیرد: هر دو
  تراکنش در لحظه COMMIT «۱» می‌شمارند، چون سطر Commit‌نشدهٔ دیگری را
  نمی‌بینند. قید معوق `single_open_opening_t` فقط پشتوانهٔ مسیرهای
  **غیرهم‌زمان** است.
  ⚠️ سندِ معکوس هم `kind = 'opening'` است، پس هر محمول «سند افتتاحیهٔ
  باز» باید `reverses_id IS NULL` داشته باشد — وگرنه معکوسِ معکوس ساخته
  می‌شود و مبالغ سند اول به دفتر برمی‌گردند.
- **سطر موجودیِ سند افتتاحیه دامنهٔ شعبه دارد، نه انبار.** سند افتتاحیه
  per branch است و جانشین، سند قبلی را **کامل** معکوس می‌کند. اگر جمع
  موجودی فقط انبار جاری را می‌دید، واردات دومِ یک شعبهٔ دو‌انباره ارزش
  انبار اول را از **دفتر** حذف می‌کرد بی‌آنکه از **انبار** برود. و
  اجرایی که سند را جانشین می‌کند باید تصویر کامل مانده‌ها را همراه
  داشته باشد، وگرنه صریح رد می‌شود.
- **`inventory.ledger_check` دفتر را با ارزش واقعی انبار تطبیق می‌دهد.**
  `balance_check` این کار را **نمی‌کند** — آن `stock_balance` را با
  `stock_movement` می‌سنجد و هر دو درون ماژول انبارند. تا مهاجرت ۰۴۹
  هیچ ثابتی نمی‌پرسید «آیا حساب موجودی کالا با انبار می‌خواند؟» و یک
  سند افتتاحیهٔ ناقص از همین شکاف رد شد. سه مصرف‌کننده دارد: ثابت‌های
  CI، `ops/deploy.sh status`، و `ops/restore-drill.sh`.
  ⚠️ **جمع کل** سنجیده می‌شود نه به‌ازای هر انبار: انتقال بین انبارها
  ارزش را جابه‌جا می‌کند و سندی نمی‌زند، پس تفکیک انباری هشدار کاذب
  می‌داد.
- **`stock_balance` Trigger تغییرناپذیری ندارد و نمی‌تواند داشته باشد** —
  `apply_movement` خودش می‌نویسدش، پس Triggerی که `UPDATE` را ببندد،
  خودِ نوشتن قانونی را هم می‌بست. ولی **نتیجه‌اش این نیست که دیتابیس
  نمی‌تواند جلویش را بگیرد.**
  مهاجرت ۰۵۰ `apply_movement` را `SECURITY DEFINER` با `search_path`
  پین‌شده کرد، پس `ops/db-roles.sh` می‌تواند حق `INSERT/UPDATE/DELETE`
  روی `stock_balance` و `stock_movement` و `cost_layer` را از **نقش
  برنامه** بگیرد و دروازه باز بماند. `db/test/db-roles.sh` هر دو جهت را
  می‌سنجد: هفت حمله رد می‌شود **و** `apply_movement` کار می‌کند.
  ⚠️ **برای نقش مالک همچنان می‌نشیند** — و مهاجرت با همان نقش اجرا
  می‌شود. پس نمای `inventory.balance_check` هنوز لازم است، به‌علاوهٔ
  `inventory.ledger_check` که دفتر را با انبار می‌سنجد. سه نگاه‌کننده:
  CI، `ops/deploy.sh status` روی سیستم زنده، و `ops/restore-drill.sh`
  روی بکاپ. `db/test/restore-drill.sql` بند ۷ ثابت می‌کند آشکارساز
  واقعاً می‌گیرد، نه اینکه همیشه خالی باشد.
  ⚠️ **تا `DATABASE_URL` برنامه به `labelmod_app` عوض نشود، هیچ‌کدام از
  این محدودیت‌ها فعال نیست.** اسکریپت نقش را می‌سازد؛ فعال‌کردنش یک
  تصمیم استقرار است.
- **هر تابعی که به سه جدول قفل‌شده می‌نویسد باید `SECURITY DEFINER` با
  `search_path` پین‌شده باشد** — و این یک **کلاس** است، نه یک فهرست.
  `db/test/write-gate.sql` کاتالوگ را می‌خواند، نه یک فهرست دستی.
  ⚠️ `SELECT … FOR UPDATE` هم حساب می‌شود: پستگرس برای قفل سطر حق
  **UPDATE** می‌خواهد، نه SELECT — و خطایش هیچ اشاره‌ای به `FOR UPDATE`
  ندارد.
  ⚠️ مهاجرت ۰۵۰ فقط `apply_movement` را بست و دو تابع جا ماندند:
  `revalue_to_cost` (که `post_receipt` صدایش می‌زند، و `costing.method`
  پیش‌فرض `last_purchase` است — یعنی **هیچ رسید خریدی ثبت نمی‌شد**) و
  `post_stock_count`. مهاجرت ۰۵۸ بستشان.
  ⚠️ **هیچ تستی قرمز نشده بود**، چون هر ۱۲۸۴ ادعای SQL و هر ۶۵۶ ادعای
  API با نقش **مالک** اجرا می‌شوند. اندازه‌گیری شد:
  `LMC_TEST_DB_ROLE=app pnpm --filter @labelmod/api test` بار اول ۳۰
  شکست داد. آن حالت، `ops/db-roles.sh` تولیدی را روی دیتابیس
  یک‌بارمصرف اجرا می‌کند و برنامه را با نقش محدود وصل می‌کند.
  ⚠️ **بکاپ و قفل‌گرفتنِ داربست با مالک می‌مانند** (`disposable.ownerUrl`):
  `pg_dump` روی `public.schema_migration` هم `LOCK TABLE` می‌زند و نقش
  برنامه آنجا حقی ندارد. همین در تولید هم هست.
  ⚠️ و `ALTER DEFAULT PRIVILEGES … REVOKE` روی کل اسکیمای `inventory`
  **برداشته شد**: پیش‌فرض به اسکیما کار می‌کند نه به نام جدول، پس هر
  جدولی که مهاجرت بعدی بسازد بی‌صدا فقط‌خواندنی می‌شد.

- **`SECURITY DEFINER` بی ACL، خودش یک ارتقای دسترسی است** — مهاجرت ۰۶۰،
  یافتهٔ FND-R60-04. ACL پیش‌فرض یک تابع در پستگرس `EXECUTE` برای
  `PUBLIC` است، پس هر شش تابع DEFINER این پروژه برای **هر نقشی که فقط
  `USAGE` روی اسکیما داشت** قابل فراخوان بودند. اندازه‌گیری شد: نقشی با
  `GRANT USAGE ON SCHEMA platform` و **هیچ حقی روی هیچ جدولی** —
  `INSERT` مستقیمش «permission denied» می‌گرفت و
  `platform.enqueue_web_push('web.stock_push', '{"onHand":9999,
  "version":999999999}')`اش **قبول می‌شد**. Worker امضایش می‌کرد و
  موجودی واقعی سایت ۹۹۹۹ می‌شد.
  ⚠️ و خرابیِ بدتر بی‌صدا بود: افزونه پیام با نسخهٔ کوچک‌تر‌یا‌مساوی را
  دور می‌اندازد، پس آن یک فراخوان هر Push **بعدیِ** آن کالا را **تا
  ابد** می‌بست. هیچ خطایی، هیچ نامهٔ مرده‌ای.
  ⚠️ **REVOKE فقط در مهاجرت بی‌اثر است.** `ops/db-roles.sh` **پس از**
  مهاجرت اجرا می‌شود و `GRANT EXECUTE ON ALL FUNCTIONS` دارد. پس سه
  کمکیِ درونی (`enqueue_web_push`، `push_web_stock`، `push_web_price`)
  در همان اسکریپت هم `REVOKE` می‌شوند — دقیقاً جایی که سه جدول قفل‌شده
  می‌شوند.
  ⚠️ **و ACL تنها لایه نیست:** `enqueue_web_push` دیگر به فراخوانش
  اعتماد نمی‌کند. موضوع از فهرست بستهٔ دو‌تایی می‌آید (`health.alert`
  **نه** — مسیر خودش را دارد)، شکل Payload سنجیده می‌شود، و `version`
  از `platform.web_push_version` گرفته می‌شود نه از ورودی. یعنی حتی
  نشستی که به تابع برسد، نسخهٔ سمّی نمی‌تواند بسازد.
  ⚠️ **و بستن یک کمکی، یک دروازه را بست.** `catalog.set_price()` خودش
  DEFINER نبود، پس با نقش برنامه فراخوانش به `push_web_price` رد
  می‌شد — یعنی **هر تغییر قیمتی در تولید ۵۰۰ می‌داد**. فقط اجرای
  `LMC_TEST_DB_ROLE=app` دیدش. مهاجرت ۰۶۰ آن را هم DEFINER کرد؛
  `set_price` از قبل معماراً دروازه بود (Trigger روی `catalog.price` هر
  نوشتن دیگری را رد می‌کند).
  ⚠️ سنجش ACL **دو حالت** دارد و یکی‌شان کافی نیست: `proacl IS NULL`
  یعنی پیش‌فرض (و پیش‌فرض همان PUBLIC است)، و `aclexplode` با
  گرانتیِ صفر یعنی PUBLIC صریح. روی دیتابیسی که `ALTER DEFAULT
  PRIVILEGES` خورده، ACL **صریح** است و `IS NULL` هیچ‌چیز نمی‌گیرد.
  `db/test/write-gate.sql` بند ۵ هر دو را می‌سنجد، با کنترل مثبتی که
  یک تابع DEFINERِ بازِ موقت می‌سازد و پس از `REVOKE` دیگر نمی‌بیندش.

- **در heredoc بی‌نقل‌قول (`<<SQL`) هرگز Backtick نگذار — حتی در کامنت
  SQL.** bash محتوای heredoc بی‌نقل‌قول را بسط می‌دهد، پس یک Backtick در
  کامنت تبدیل به Command Substitution می‌شود. `ops/db-roles.sh` ۱۳ سطر
  کامنت این‌شکلی داشت و خروجی‌اش ۹ خط `command not found` به‌علاوهٔ یک
  `syntax error: unexpected end of file` بود — یعنی **متنی که به psql
  می‌رسید همان چیزی نبود که در فایل نوشته شده**. آنجا کامنت بود و
  بی‌ضرر؛ یک Backtick در خودِ دستور `REVOKE` بی‌صدا حذفش می‌کرد و یک
  اسکریپت امنیتی «موفق» گزارش می‌شد. داخل heredoc از « » استفاده کن.
  heredoc را می‌شود نقل‌قول کرد (`<<'SQL'`) ولی آن‌وقت `$APP_ROLE` هم
  بسط نمی‌شود، پس در این فایل انتخاب، حذف Backtick بود.

- **`ADD CONSTRAINT … CHECK` روی جدول پرسطر، دو مرحله‌ای است:
  `NOT VALID` و بعد `VALIDATE CONSTRAINT`** — یافتهٔ FND-R60-05.
  `ADD CONSTRAINT` یک‌مرحله‌ای کل جدول را زیر `ACCESS EXCLUSIVE` اعتبارسنجی
  می‌کند: هر `SELECT`، هر فروش، هر اسکن تا پایانش **مسدود** است.
  `VALIDATE CONSTRAINT` جداگانه فقط `SHARE UPDATE EXCLUSIVE` می‌گیرد و
  فروش ادامه دارد.
  ⚠️ مهاجرت ۰۵۹ همین را یک‌مرحله‌ای نوشت روی `inventory.stock_movement` —
  جدولی که **بیشترین رشد** را در کل اسکیما دارد. روی دیتابیس خالیِ CI
  میکروثانیه است و هیچ تستی قرمز نمی‌شود؛ روی انبار دو‌ساله همان دستور
  صندوق را برای مدتی می‌خواباند. **۰۵۹ ویرایش نمی‌شود** (هشش در
  `public.schema_migration` نشسته) — این قاعده برای مهاجرت‌های بعدی است،
  و راهنمای اجرای امنِ ۰۵۹ روی داده واقعی در `docs/DEPLOYMENT.md` بند
  ۱۲ است.
  ⚠️ و `lock_timeout` را صریح ست کن، نه اینکه امیدوار باشی: بی آن،
  مهاجرت تا هر زمانی در صف قفل می‌ماند و **در همان مدت هر درخواست
  تازه‌ای هم پشتش صف می‌کشد** — یک قفل ناگرفته، کل جدول را می‌خواباند.

- **مهاجرت موجود ویرایش نمی‌شود** — و حالا `ops/db.sh migrate` هم
  اجبارش می‌کند: هش هر فایلِ اجراشده در `public.schema_migration`
  می‌نشیند و ویرایشِ بعدی، مهاجرت را با پیام صریح متوقف می‌کند. ۰۰۳ و ۰۰۴ توابع قبلی را با
  `CREATE OR REPLACE` جایگزین کرده‌اند؛ نسخه معتبر هر تابع آخرین تعریف
  آن است. برای دیدن نسخه جاری: `\sf sales.post_batch`
- **داده مرجع در `db/seed/` است، نه در مهاجرت.** بعد از هر مهاجرتی که
  حساب یا قاعده ثبت جدید لازم دارد، `ops/db.sh seed` را دوباره اجرا کن.
  Seed حالا Idempotent است: سطر تازه اضافه می‌شود و سطر موجود
  دست‌نخورده می‌ماند — پس مقدار تصویب‌شده حسابدار با اجرای دوباره به
  پیش‌فرض برنمی‌گردد.
- هوک `pre-push` هم راز می‌گیرد، هم تست اجرا می‌کند. اگر `DATABASE_URL`
  ست نباشد فقط هشدار می‌دهد و تست را رد می‌کند — این را عادی نگیر.
- مخزن: `AliJ021/labelmod-core` (خصوصی، Free). **Branch protection،
  Rulesets و Secret scanning در دسترس نیستند** — آزموده شد، نه فرض.
  هوک محلی جایشان را گرفته، پس روی هر کلون `ops/install-hooks.sh`.
- سمت سرور این‌ها فعال‌اند و قابل دور زدن نیستند: اجبار پین اکشن به SHA،
  فهرست مجازِ محدود به اکشن‌های خود GitHub، توکن CI فقط‌خواندنی،
  Dependabot. جزئیات در `docs/SECURITY.md` بند ۹.
- **اکشن جدید فقط با SHA کامل.** `@v4` باعث شکست کل Workflow می‌شود.
  SHA را با `git ls-remote https://github.com/actions/<نام> refs/tags/v*`
  بگیر، نه از حافظه.
- **نسخه وابستگی از حافظه انتخاب نکن.** `pnpm audit --prod
  --audit-level high` در CI است و بیلد را می‌شکند. نسخه‌های اولیه همین
  ماژول پنج آسیب‌پذیری High داشتند که سه‌تایش تزریق SQL بود.
- مهاجرت‌ها SQL خام و شماره‌دارند. هرگز مهاجرت تولیدشده توسط ORM اضافه نکن.

## ساختار

```
db/migrations/  اسکیما و توابع — SQL خام، شماره‌دار، قابل حسابرسی
db/seed/        کدینگ حساب، قواعد ثبت، تنظیمات، داده مرجع
db/test/        تست‌های مالی — در CI و در pre-push اجرا می‌شوند
db/reconcile/   تطبیق پایانی «یک روز کامل» — عمداً بیرون از گلوب db/test
docs/           ADR-001 (Stack) · ADR-002 (طراحی) · ADR-003 (دوره ثبت)
                ADR-004 (چک) · ADR-005 (اعتماد دستگاه)
                ADR-006 (قیمت تمام‌شده) · SECURITY.md · DEPLOYMENT.md
ops/            db.sh · deploy.sh · install-hooks.sh · hooks/
                deploy/ — Caddyfile، Dockerfile زمان‌بند، scheduler.sh
integrations/   افزونه ووکامرس (PHP) — سفارش سایت و خوراک موجودی
apps/api/       Fastify + Kysely — احراز هویت، نشست، مجوز، فروش، صندوق،
                مرجوعی، دوره ثبت، کالا، سفارش و رسید خرید، برگشت از
                خرید، انبارگردانی، تنظیمات
apps/web/       React + Vite — داشبورد، صندوق، مرجوعی، کالا و قیمت،
                انبار و خرید، خزانه و چک، تنظیمات (+ زبانه‌های دستگاه‌ها،
                نگاشت حساب، و سلامت سیستم)
apps/api/src/worker/  صف Outbox، پیامک، هشدار سررسید چک
apps/api/src/import/  مهاجرت داده از سیستم فعلی — CSV با ستون ثابت
```

## قواعد لایه API

جزئیات در `.claude/rules/api.md` و `docs/SECURITY.md`. سه‌تا که بیشترین
اهمیت را دارند:

- **پول در JSON رشته است، نه عدد.** `apps/api/src/lib/money.ts` تنها
  مرز تبدیل است و `number` را صریح رد می‌کند.
- **قیمت از دیتابیس می‌آید، نه از کلاینت.** کلاینت فقط کالا و تعداد را
  می‌گوید؛ `unit_price` از `catalog.price` خوانده می‌شود. تخفیف یک
  میدان جداگانه و مجوزدار است، نه یک قیمت کمتر.
- **محاسبه پولی در SQL، نه در TypeScript.** `qty × price` با `round()`
  صریح در دیتابیس حساب می‌شود. تقسیم صحیح bigint برای تعداد اعشاری
  نتیجه‌ای می‌دهد که با `sum()` دیتابیس یکی نیست.
- **هر Endpoint تغییردهنده وضعیت `Idempotency-Key` می‌پذیرد.**
  `lib/idempotency.ts` درج Inbox را **پیش از** اثر و در همان تراکنش
  انجام می‌دهد.
- **هیچ شرط دسترسی در کد نیست.** مجوز فقط از `identity.can()`. اگر
  وسوسه شدی یک `if` روی نام نقش بنویسی، یعنی یک ردیف در
  `permission_rule` کم است.
- **نگهبان دیتابیس ۴۰۹ می‌دهد، نه ۵۰۰.** هر `RAISE EXCEPTION` در
  توابع ما `SQLSTATE = P0001` می‌گیرد و پیامش فارسی و برای کاربر است.
  `errors.ts` این‌ها را به `rule_violation` ترجمه می‌کند. دفاعی که
  شبیه خرابی سرور گزارش شود، در عمل خاموش است.
- **کاربر عامل در همان تراکنش.** `platform.set_actor()` با
  `is_local = true` ست می‌شود، پس در Pool اشتراکی به درخواست بعدی نشت
  نمی‌کند — و به همین دلیل فقط داخل تراکنش معنا دارد.
- **بازپرداخت نقدی بدون شیفت باز ثبت نمی‌شود.** `sales.post_return`
  یک `treasury.payment` با `shift_id` برگ مرجوعی می‌سازد و
  `sales.close_shift` خروجی نقد را از همان‌جا می‌خواند. اگر آن
  `shift_id` تهی یا متعلق به شیفت بسته باشد، پول از کشو رفته ولی در
  شمارش نیامده — یک مغایرت کاذب که هیچ‌کس نمی‌تواند توضیحش دهد.
  دیتابیس این را نمی‌داند؛ دروازه‌اش در `http/return-routes.ts` است.
- **«کدام کشو» یک تعریف دارد: `treasury/cash-drawer.ts`.** و کشو از
  **شعبه** پیدا می‌شود، نه از کاربر عامل — هم برای حرکت نقد غیرفروشی و
  هم برای بازپرداخت مرجوعی.
  ⚠️ مسیر مرجوعی نسخه دومِ این دروازه را داشت که `shifts.current(userId,
  branch)` می‌خواست. و چون `refund.cash` را صندوق‌دار **ندارد** (Seed،
  بند ۱ SECURITY.md)، نتیجه‌اش این بود که در یک روز عادی با یک کشو
  **هیچ‌کس** نمی‌توانست بازپرداخت نقدی بزند: صاحب کشو ۴۰۳ و مجوزدارِ
  بی‌کشو ۴۲۲. راه دور زدنش (کشوی دوم برای سرپرست) بدتر بود — بازپرداخت
  روی کشوی اشتباه می‌نشست و **دو** مغایرت کاذب می‌ساخت، از جمله یک
  کشو با «انتظار نقد منفی». اندازه‌گیری شد، نه حدس.
  با **دو** کشوی باز، `POST /returns` باید `shiftId` بگیرد؛ نام‌نبردنش
  ۴۲۲ `ambiguous_shift` است. حدس‌زدن یعنی مغایرت را از یک صندوق‌دار به
  دیگری منتقل کنیم. `db/test`ی ندارد چون دیتابیس نمی‌داند کدام کاربر
  کدام کشو را باز دارد — `apps/api/test/cash-drawer.integration.test.ts`.
- **ستون `date` را صریح `::text` کن.** درایور آن را به `Date`
  جاوااسکریپت می‌کند و `JSON.stringify` یک timestamp کامل می‌نویسد.
  یک بار همین، مقایسه «آیا این دوره مالِ امروز است؟» را برای همیشه
  نابرابر کرد — یعنی نگهبانی که نوشته شده بود، خاموش بود.
- **گزارش با دفتر یکی است، وگرنه گزارش نیست.** هر تابع
  `report_*` در برابر همان منبعی سنجیده می‌شود که باید از آن بیاید:
  فروش در برابر `sales.daily_summary`، ارزش موجودی در برابر
  `inventory.stock_balance`، تراز در برابر توازن سند. عددی که فقط
  تست خودش حسابش کرده باشد، هیچ‌چیز را اثبات نمی‌کند.
- **«فاکتور واقعی» چهار وضعیت دارد، نه دو تا:** `finalized`، `paid`،
  `partially_returned`، `returned`. `post_return` وضعیت فاکتور را عوض
  می‌کند، پس فیلتر دوتایی یعنی هر فاکتوری که مرجوعی خورده **کاملاً**
  از گزارش فروش بیفتد.
- **دامنه شعبه در گزارش کار مسیر است، نه تابع.** `p_branch IS NULL`
  در توابع یعنی «همه شعبه‌ها»؛ اگر مسیر پارامتر نداشتِ کلاینت را
  مستقیم بدهد، حذف یک Query String دامنه را باز می‌کند.
  `resolveBranch()` در `report-routes.ts` تصمیم می‌گیرد.
- **دوره امروز از داشبورد بسته نمی‌شود.** بستنش یعنی هر فروش بعدیِ
  همان روز و همان کانال با «دوره ثبت این فاکتور قبلاً بسته شده است»
  رد شود — همان دلیلی که کار شبانه هم امروز را رد می‌کند. «امروز» از
  پاسخ سرور می‌آید نه از ساعت مرورگر. دوره **شیفت** هم از آنجا بسته
  نمی‌شود؛ با شمردن کشو بسته می‌شود. `canClosePeriod` در
  `apps/web/src/lib/pos.ts` و تستش در `apps/web/test/posting.test.ts`.
- **بستن دوره کانال، کلید Idempotency خودش را می‌سازد.** هویت آن
  عملیات همان (شعبه، کانال، تاریخ) است، نه هدری که کلاینت می‌فرستد —
  پس هدر عمداً نادیده گرفته می‌شود. پذیرفتنش تنها راهِ دور زدن قفل بود.
- **«امروز» یک تعریف دارد، نه چند تا.** `platform.business_date()` از
  تنظیم `platform.timezone` (پیش‌فرض `Asia/Tehran`) می‌آید، نه از
  منطقه زمانی سرور. بدون این، سفارش یک بامداد روی سروری با UTC به روز
  قبل می‌خورد. نوع `timezone` مقدار را در برابر `pg_timezone_names`
  می‌سنجد — غلط تایپی در نام منطقه، هر فروش را می‌شکست.
- **کار شبانه به نام کاربر «سیستم» ثبت می‌شود.** `seed` می‌سازدش و
  هرگز نمی‌تواند وارد شود: نه رمز، نه PIN، `is_active` خاموش. فقط یک
  نام برای سندی که آدمی پشتش نیست.
- **بستن دوره، موجودی را دست نمی‌زند.** کالا در همان لحظه
  `finalize_invoice` از انبار خارج می‌شود؛ آنچه بعداً بسته می‌شود فقط
  **سند حسابداری** درآمد و بهای تمام‌شده است. اگر روزی کسی این دو را
  یکی کرد، `db/test/auto-close.sql` قرمز می‌شود.
- **دوره کانال آنلاین شبانه و خودکار بسته می‌شود** —
  `sales.close_due_channel_days()` از راه
  `POST /posting-batches/close-due` یا `ops/close-due-days.sh` در cron.
  دوره **امروز** هرگز بسته نمی‌شود و دوره مشکل‌دار رد می‌شود نه اینکه
  کل اجرا را بشکند. کلیدش `sales.auto_close_channel_day` است.
  `GET /posting-batches/unposted` همچنان زنگ خطر است.
- **بارکد داخلی EAN-13 با پیشوند ۲۰ است.** محدوده «گردش محدود در یک
  شرکت» در GS1، پس هرگز با بارکد کارخانه اشتباه نمی‌شود. رقم کنترل
  یعنی برچسب خط‌خورده **خطا** می‌دهد، نه کالای دیگری.
- **رسم بارکد رمزگشا هم دارد، و تست از روی خروجی می‌خواند.** جدول
  L/G/R را می‌شود اشتباه رونویسی کرد و نتیجه باز هم «شبیه بارکد» باشد.
  `decodeEan13Modules` و بازخوانی هندسه از `<rect>`های SVG، رفت‌وبرگشت
  را واقعی می‌سنجند. پیش از چاپ اولین رول واقعی، **یک بار با
  بارکدخوان فیزیکی هم امتحان کنید** — هیچ تستی جای آن را نمی‌گیرد.
- **صفحه برچسب تنها خروجی HTML این پروژه است.** نام کالا و رنگ از
  دیتابیس می‌آیند ولی ریشه‌شان ورودی کاربر است، پس دو لایه دفاع دارد:
  `esc()` روی هر درج، و هدر CSP که `script-src` را کامل می‌بندد. صفحه
  اصلاً جاوااسکریپت لازم ندارد.
- **ترتیب سایز از `catalog/size-order.ts` می‌آید، نه از `sort()`.**
  الفبایی یعنی `L, M, S, XL` — و ماتریس موجودی بدون ستون‌های مرتب،
  «سوراخ وسط جدول» را نشان نمی‌دهد که تنها دلیل وجودش است.
- **صفحه تنظیمات هیچ کلیدی را نمی‌شناسد.** برچسب، ویجت، گزینه‌ها،
  بازه و «آیا این کاربر می‌تواند عوضش کند» همه از `GET /settings`
  می‌آیند. `canEdit` هم برای هر **عملیات متمایز** یک بار حساب می‌شود،
  نه یک بار برای هر تنظیم.
- **اعتبارسنجی تنظیم در دیتابیس است، نه در Zod.** بدنه `PATCH` عمداً
  `value` را `unknown` می‌گیرد. اگر Zod هم می‌سنجید، دو نسخه از یک
  قاعده داشتیم — و آن که در psql دور زده می‌شود همان است که اهمیت
  دارد. سنجش سمت مرورگر (`lib/settings-value.ts`) فقط برای بازخورد
  فوری است و بازه‌هایش را از خودِ پاسخ سرور می‌خواند.
- **ورودی عددی فرم `type="text"` است، نه `type="number"`.** صفحه‌کلید
  فارسی «۴۸» می‌فرستد و ورودی عددی مرورگر آن را دور می‌اندازد.
  `normalizeDigits` رقم فارسی و عربی و جداکننده هزارگان را می‌فهمد.
- **قیمت سایت از کلاینت می‌آید — و این تنها استثناست.** مشتری همان
  چیزی را پرداخت کرده که سایت نشانش داده؛ ثبت قیمت جاری `catalog.price`
  یعنی درآمدی در دفتر که هرگز دریافت نشده. ولی «قیمت آزاد» نیست: از
  همان دروازه `sales/markdown-gate.ts` می‌گذرد که صندوق از آن می‌گذرد،
  با دلیل ثابت «قیمت سایت». **دروازه یک تعریف دارد، نه دو تا** — کپی
  کردنش یعنی یکی‌شان عقب بماند، و همان است که دور زده می‌شود.
- **اتصال سایت با کلید است، نه با SKU و نه با نام.** نام کالا در سایت
  برای سئو نوشته می‌شود و عمداً با نام انبارداری فرق دارد. خوراک
  `variationId` می‌فرستد و افزونه در متای `_lmc_variation_id`
  نگهش می‌دارد؛ SKU فقط یک پل **یک‌بارمصرف** برای برقراری اتصال اولیه
  است. قیمت هم از همان خوراک می‌رود، ولی `null` یعنی «قیمت ندارد» نه
  «مجانی» — نوشتن صفر یعنی ویترین کالا را رایگان بفروشد.
- **خرید حضوری در سایت `shop_order` نمی‌شود.** رکورد در نوع پست جدای
  `lmc_instore_purchase` می‌نشیند. `shop_order` یعنی موجودی دوباره کم
  شود، ایمیل برود، در گزارش سایت دوباره شمرده شود، و از همه بدتر یک
  **حلقه بازگشتی** بسته شود چون همان Hookهایی را می‌زند که افزونه به
  آن‌ها گوش می‌دهد. سه نگهبان دارد: `channel <> 'web'` در سرور، همان
  سنجش در افزونه، و اینکه رکورد اصلاً سفارش نیست.
- **شماره مشتری پای صندوق اختیاری است و از دیتابیس نرمال می‌شود.**
  `sales.normalize_mobile` تنها تعریف است. ورودی‌ای که شماره نیست
  **پیش از درج** رد می‌شود: `mobile_normalized` می‌تواند NULL باشد و
  قید یکتایی روی NULL اعمال نمی‌شود، پس هر غلط تایپی یک مشتری تازه و
  بی‌شماره می‌ساخت.

- **خروجی CSV از همان مسیر گزارش می‌آید، نه از مسیری جدا.**
  `?format=csv` روی همان Endpoint، و پوشاندن بها **پیش از** تصمیم
  قالب انجام می‌شود — پس فایل نمی‌تواند چیزی را لو بدهد که صفحه
  نمی‌دهد. مسیر دانلود جدا یعنی دو نسخه از همان دروازه‌ها.
- **CSV سه تله دارد که همه‌شان بی‌صدا هستند.** بدون **BOM** اکسل
  ویندوز فارسی را نامفهوم نشان می‌دهد؛ سلولی که با `=`، `+`، `-`،
  `@`، Tab یا CR شروع شود برای اکسل **فرمول** است و نام کالا ورودی
  کاربر است (تزریق CSV، بیرون از مرورگر و بدون هیچ CSP)؛ و پول باید
  رشته بماند. `apps/api/src/lib/csv.ts` هر سه را می‌بندد.
  ⚠️ نقل‌قول کردن کافی نیست: اکسل `"=cmd"` را هم فرمول می‌بیند.

- **کلید API یک کاربر پشتی دارد، نه یک سیستم Scope جدا.** مجوز از همان
  `permission_rule` می‌آید و `platform.audit()` همان کاربر را می‌نویسد.
  کاربر پشتی مثل «سیستم» هرگز وارد نمی‌شود: نه رمز، نه PIN،
  `is_active` خاموش. ابطال کلید یک `UPDATE` است، نه یک `DELETE`.
- **CSRF روی مسیر کلید API اعمال نمی‌شود، و این درست است.** CSRF حمله‌ای
  مبتنی بر کوکی است؛ هدر `Authorization` را هیچ مرورگری خودکار
  نمی‌فرستد. اعمالش فقط سایت را از کار می‌انداخت.
- **کلید Idempotency سفارش سایت از شماره سفارش ووکامرس ساخته می‌شود**،
  نه از هدر کلاینت — همان قاعده بستن دوره کانال. ووکامرسی که دو بار
  Webhook بزند باید Replay بگیرد، نه فاکتور دوم.
- **`addLineIn` / `addPaymentIn` / `finalizeIn` خواندن‌هایشان روی همان
  تراکنش است، نه روی Pool.** مسیر سفارش سایت کل فاکتور را در یک تراکنش
  می‌سازد؛ کدی که داخل تراکنش بنویسد و بیرونش بخواند، پیش‌نویسِ
  Commit‌نشده را نمی‌بیند و با «فاکتور یافت نشد» رد می‌شود — خطایی که
  هیچ ربطی به واقعیت ندارد. یک بار همین اتفاق افتاد.
- **صف Outbox اجاره دارد، نه قفل.** `platform.claim_outbox()` پیام را
  `sending` می‌کند و `next_attempt_at` را جلو می‌برد؛ Workerی که وسط
  ارسال بمیرد، پس از پایان مهلت دوباره برداشته می‌شود. **تحویل
  «حداقل یک بار» است، نه «دقیقاً یک بار»** — هر Handler باید تکرار را
  تحمل کند.
- **خاموش‌بودن یک شکست نیست.** فروش ناشناس، پیامک خاموش و مشتری بدون
  رضایت همه با `complete_outbox` بسته می‌شوند، نه `fail_outbox`. اگر
  ناموفق می‌شدند، `platform.outbox_dead` پر می‌شد از چیزهایی که قرار
  نبود بروند و خطای واقعی همان‌جا گم می‌شد.
- **خطای «رد شد» از «نرسید» جدا است.** `SmsError.permanent` مستقیم
  نامه مرده می‌سازد؛ بقیه Backoff می‌گیرند. شماره غلط با تلاش صدم هم
  درست نمی‌شود، و قطعی لحظه‌ای شبکه نباید یک پیامک را برای همیشه
  بکشد.
- **PDF با کتابخانه ساخته نمی‌شود؛ مرورگر می‌سازدش.** پیامک ضمیمه
  نمی‌گیرد و شکل‌دهی حروف فارسی در کتابخانه‌های PDF جاوااسکریپت یا
  نیست یا شکسته است. صفحه `/i/<توکن>` حالا دکمه «چاپ یا ذخیره PDF»
  و استایل `@media print` دارد؛ موتور شکل‌دهی فارسیِ خودِ مرورگر کار
  را می‌کند.
  ⚠️ آن دکمه یک بلوک `<script>` است و CSP این صفحه `unsafe-inline`
  **ندارد** — فقط یک hash از متن دقیق اسکریپت. اگر آن متن یک
  کاراکتر عوض شود و hash نه، دکمه **بی‌صدا** کار نمی‌کند و هیچ
  خطایی در کنسول کاربر نیست. `apps/api/test/invoice-page.test.ts`
  هم‌خوانی‌شان را قفل کرده. hash روی `onclick` اصلاً اعمال نمی‌شود.
- **لینک فاکتور از `platform.public_url` ساخته می‌شود، نه از هدر
  `Host`.** Worker درخواستی ندارد که هدری داشته باشد، و هدر `Host` را
  کلاینت می‌فرستد — لینکی که از آن ساخته شود به دامنه‌ای می‌رفت که
  مهاجم انتخاب کرده.
- **کلید سرویس پیامک در `SMS_API_KEY` است، نه در `platform.setting`.**
  مقدار هر تنظیم در `audit_log` می‌نشیند و صفحه تنظیمات نشانش می‌دهد؛
  کلید یک راز است. انتخاب سرویس‌دهنده و شماره فرستنده اما تصمیم‌اند و
  در جدول‌اند.
- **مهاجرت داده پیش‌فرضش آزمایشی است.** بدون `--commit` هیچ‌چیز نوشته
  نمی‌شود. پنج فایل با هم سنجیده می‌شوند و در **یک تراکنش** نوشته —
  وارداتِ نیمه‌کاره از واردات‌نشده بدتر است.
- **سرمایه در سند افتتاحیه محاسبه می‌شود، نه وارد.** دارایی منهای
  بدهی. `inventory` و `equity` عمداً در فایل ورودی نیستند: اولی از
  فایل موجودی می‌آید و دومی رقم متوازن‌کننده است.
- **بهای تمام‌شده موجودی افتتاحیه اجباری است.** موجودی بدون بها یعنی
  اولین فروشِ آن کالا سودِ صددرصد نشان می‌دهد و ترازنامه دارایی‌ای
  دارد که ارزشش صفر است.
- **پارامتر بدون Cast در `substring(x from $1)` نسخه Regex را
  برمی‌گزیند، نه موقعیتی.** یک بار همین بی‌صدا شماره‌گذاری SKU را از نو
  شروع کرد. هر جا موقعیت عددی لازم است، یا Literal بنویس یا `::int`.

## قواعد انبار و خرید

- **قیمت خرید از کلاینت می‌آید — و باید بیاید.** برعکسِ فروش. قیمت
  خرید تصمیم تأمین‌کننده است و هیچ‌جا در دیتابیس ما نیست؛ انباردار از
  روی فاکتور کاغذی واردش می‌کند. محافظ جای دیگری است: رسید تا ثبت
  هیچ اثری ندارد، ثبتش `stock.receive` می‌خواهد، و پس از ثبت
  تغییرناپذیر است.
- **شماره رسید و برگه شمارش در لحظه ثبت تخصیص می‌یابد**، نه هنگام
  ساخت پیش‌نویس (مهاجرت ۰۲۵ و ۰۲۷). پیش‌نویس رهاشده نباید شماره
  بسوزاند. تخصیص داخل همان تابع و روی همان سطر قفل‌شده است.
- **همان کالا با همان قیمت روی یک سطر رسید جمع می‌شود؛ با قیمت
  متفاوت، سطر تازه.** دو نرخ روی یک فاکتور تأمین‌کننده، دو سطر
  واقعی‌اند.
- **هزینه با تخصیص `none` واقعاً به بهای کالا نمی‌رود** (مهاجرت ۰۲۶).
  تا آن مهاجرت، باقی‌مانده گرد کردن تمامش را روی آخرین قلم می‌گذاشت.
  حالا سطر سند خودش را دارد (`expensed_charge`) و سرفصلش انتخابی است.
- **موجودی سیستم در انبارگردانی، در لحظه ثبت خوانده می‌شود.** نه
  هنگام ورود سطر، نه در لایه API. شمارش ساعت‌ها طول می‌کشد و فروشگاه
  باز است؛ عدد کهنه یعنی کسری کاذب. `system_qty` را فقط
  `inventory.post_stock_count()` پر می‌کند و تا آن لحظه `NULL` است.
- **صفحه انبارگردانی موجودی سیستم را پیش از ثبت نشان نمی‌دهد.** اگر
  انباردار جواب را ببیند، نمی‌شمارد — تأیید می‌کند.
- **کالای شمرده‌نشده صفر نمی‌شود.** انبارگردانی جزئی کار عادی است؛
  اگر نبودِ سطر «صفر» تعبیر شود، اولین شمارش جزئی کل انبار را پاک
  می‌کند.
- **شمارش مطلق است، نه افزایشی.** اسکن دوباره یعنی «دوباره شمردم و
  این عدد است». به همین دلیل مسیرش `PUT` است و `Idempotency-Key`
  نمی‌خواهد.
- **در برگشت از خرید، بهای فاکتور و ارزش دفتری یکی نیستند.** بدهی
  تأمین‌کننده به اندازه `unit_price` کم می‌شود و موجودی به اندازه
  `landed_unit_cost`. تفاوتشان حملِ کالای پس‌فرستاده است — یک زیان
  واقعی با سرفصل خودش (۵۱۰۲)، نه چیزی که در بدهی یا ارزش موجودی گم
  شود.
- **کالای برگشتی با بهای همان رسید خارج می‌شود**، نه میانگین جاری.
  در روش «آخرین قیمت خرید» هر خرید بعدی کل موجودی را تجدید ارزیابی
  می‌کند؛ بدون Snapshot سطر رسید، کالایی که به نرخ ۱۰۰ آمده بود به
  نرخ ۱۲۰ برمی‌گشت.
- **انتقال بین انبارها یک رویداد مالی نیست، ولی ارزش را جابه‌جا
  می‌کند.** هر دو انبار به حساب ۱۳۰۱ می‌خورند، پس سندی زده نمی‌شود.
  ولی خروج و ورود باید **دقیقاً** یک ارزش داشته باشند:
  `post_transfer` مقدار `value_delta` برگشتی از خروج را با علامت
  مخالف به ورود می‌دهد، نه `qty × unit_cost`. وقتی انبار مبدأ خالی
  می‌شود، `apply_movement` باقی‌ماندهٔ گرد کردن را هم صفر می‌کند و آن
  دو عدد یکی نیستند — ضرب دوباره، چند ریال را از ترازنامه گم می‌کرد.
- **دامنه انتقال روی هر دو انبار سنجیده می‌شود.** سنجش فقط مبدأ، راهی
  است برای بیرون‌بردن کالا به انباری که کاربر نمی‌بیند. سه سنجش در
  `transfer-routes.ts`: شعبه برگه، انبار مبدأ، انبار مقصد.
- **بارکد یک تعریف دارد: `catalog/resolve.ts`.** رسید خرید،
  انبارگردانی و انتقال همه از آن می‌گذرند، پس «کالای بایگانی‌شده اسکن
  نمی‌شود» یک جا نوشته شده نه سه جا.
- **جست‌وجوی رسید از روی شماره سمت سرور است** (`GET /receipts/lookup`).
  فیلتر روی فهرست کافی نیست: فهرست سقف دارد و رسید سه ماه پیش در آن
  نمی‌آید.
- **سفارش خرید یک تعهد است، نه یک رویداد مالی.** هیچ سندی نمی‌زند و
  هیچ حرکت انباری نمی‌سازد. سیستمی که سفارش را در دفتر بنشاند،
  ترازنامه‌ای می‌سازد که کالای نرسیده را دارایی می‌بیند. جمع سفارش
  یک عدد **برای مقایسه** است، نه یک بدهی — و صفحه هم همین را
  می‌نویسد.
- **«چقدرش رسیده» ستون نیست، نمای `purchasing.order_progress` است** و
  برگشتی را کم می‌کند. یک ستون مشتق یعنی دو تابع باید یک عدد را
  هم‌زمان درست نگه دارند.
- **بستن سفارش یک تصمیم انسانی است**، نه نتیجه یک محاسبه؛ و بستن
  سفارشِ نیمه‌رسیده **دلیل می‌خواهد** — دیتابیس اجبارش می‌کند.
- **بیش‌تحویل بسته نمی‌شود.** بستنش یعنی انباردار نتواند محموله واقعی
  را ثبت کند و کالای در قفسه در سیستم نباشد — بدتر از خودِ بیش‌تحویل.
  در نما با `over_qty` دیده می‌شود.

## قواعد رابط کاربری

جزئیات در `.claude/rules/design.md` و `docs/ADR-002-design-language.md`.
چهارتا که در کد اجبار شده‌اند:

- **`<Glass>` خودش نمی‌گذارد شیشه روی شیشه بیفتد.** اگر داخل یک سطح
  شیشه‌ای باشد، خودبه‌خود به تینت تبدیل می‌شود. قاعده‌ای که فقط در سند
  باشد، دیر یا زود نقض می‌شود — یک بار شد.
- **جهت رابط راست‌چین است — و در مرورگر واقعی سنجیده شد، نه با grep.**
  Build تولیدی با CSP تولیدی، ورود واقعی، ۹ صفحه × ۷ عرض: `dir="rtl"`،
  `direction: rtl`، و **صفر** بلوکِ بزرگی که جهتش با ریشه فرق کند.
  `apps/web/test/direction.test.ts` سمتِ ایستایش را قفل می‌کند: هیچ
  خاصیت فیزیکیِ جهت‌دار در CSS، هیچ استایل درون‌خطیِ جهت‌دار در TSX، و
  فهرست قفل‌شدهٔ جاهایی که عمداً `dir="ltr"` دارند (عدد، شناسه، نشانی —
  همیشه **برگ**، هرگز ظرف).
  ⚠️ **شکاف Breakpoint، اسکرول افقی می‌سازد.** `.zones` تا ۵۶۰px مهار
  می‌شد و چیدمان دسکتاپ از ۹۰۰px شروع می‌شود؛ بازهٔ ۵۶۱–۸۹۹ بی‌قاعده
  بود و **کل صفحه** ۱۰۵ پیکسل افقی اسکرول می‌خورد — روی هر نه صفحه، و
  دقیقاً روی ۷۶۸ که تبلت عمودی صندوق است. تست همین شکاف را عددی
  می‌سنجد.
  ⚠️ `min-width: 0` روی آیتم Flex اجباری است: بی‌آن `overflow-x` بی‌اثر
  است، چون آیتم هرگز کوچک‌تر از محتوایش نمی‌شود.
- **سطر سبد صندوق `memo` با مقایسه‌کنندهٔ دستی دارد، و سه تکه‌اش با هم
  کار می‌کنند** (FND-016): مقایسه‌کننده، دیسپچر پایدارِ
  `useMemo(..., [])`، و `fieldset[disabled]` به‌جای `disabled={busy}`
  روی هر دکمه. هر سه وقتی می‌شکنند که صفحه همچنان **درست** کار می‌کند و
  فقط کند می‌شود، پس `apps/web/test/pos-cart-memo.test.ts` ساختار را
  قفل کرده — از جمله اینکه هر میدانی که سطر می‌کشد در مقایسه‌کننده هم
  باشد، وگرنه سطر بی‌صدا **به‌روز نمی‌شود**.
  ⚠️ `memo` بی مقایسه‌کننده اینجا **بی‌اثر** است: `setInvoice(out.invoice)`
  کل فاکتور را از پاسخ سرور می‌نشاند، پس هر سطر یک شیء تازه است و
  مقایسهٔ سطحی همیشه «عوض شده» می‌گوید.
- **پالت در CI سنجیده می‌شود.** `apps/web/test/palette.test.ts` توکن‌ها
  را از خودِ `tokens.css` می‌خواند و تضاد و ΔE را می‌سنجد. عوض‌کردن یک
  Hex بدون این تست، یعنی شکستن یک ادعای سنجیده‌شده.
- **نمودار روی شیشه نمی‌نشیند.** رنگ نمودار در تم روشن روی شیشه فقط
  ۲٫۷۷:۱ می‌دهد؛ روی سطح مات ۳٫۵۳:۱. سنجیده شده، نه سلیقه.
- **حالت عملکرد `backdrop-filter` را واقعاً برمی‌دارد**، نه فقط بلور را
  صفر می‌کند — `url(#…)` خودش یک لایه ترکیب می‌سازد.

## قواعد استقرار

جزئیات در `docs/DEPLOYMENT.md`. پنج‌تا که شکستنشان بی‌صداست:

- **`wasm-unsafe-eval` از CSP برداشته نمی‌شود.** اسکنر بارکد روی هر
  مرورگری بدون `BarcodeDetector` بومی — **از جمله آیفون** — از
  WebAssembly استفاده می‌کند. بدون آن، دوربین باز می‌شود، تصویر
  می‌آید، و هیچ بارکدی خوانده نمی‌شود؛ هیچ خطایی هم در کنسول نیست.
  `apps/web/test/csp.test.ts` این را قفل کرده.
- **`index.html` اسکریپت درون‌خطی نمی‌گیرد.** CSP تولیدی
  `unsafe-inline` ندارد و Caddy فایل ایستا را از دیسک سرو می‌کند، پس
  nonce ممکن نیست. بوت تم در `apps/web/public/boot.js` است. به همین
  دلیل `build.modulePreload.polyfill` هم خاموش است — آن Polyfill یک
  اسکریپت درون‌خطی تزریق می‌کند.
- **UI و API روی یک دامنه‌اند.** کوکی نشست `SameSite=Strict` است و
  کلاینت `/api` را نسبی صدا می‌زند. دو دامنه یعنی کوکی اصلاً فرستاده
  نمی‌شود و ورود بی‌هیچ پیام مفیدی کار نمی‌کند.
- **پستگرس و API هیچ پورتی روی میزبان منتشر نمی‌کنند.** کوکی نشست در
  تولید `Secure` است؛ یک مسیر HTTP لخت به API یعنی همان کوکی روی
  شبکه رمزنشده. تنها راه ورود، Caddy است.
- **`seed` هیچ حساب انسانی نمی‌سازد.** تنها کاربرش «سیستم» است که نه
  رمز دارد نه PIN و غیرفعال است. اولین مدیر با
  `apps/api/src/cli/create-user.ts` ساخته می‌شود و رمزش تصادفی است و
  یک بار چاپ می‌شود. رمز پیش‌فرضِ کامیت‌شده همان چیزی است که اولین
  سطر فهرست کنترل بند ۸ SECURITY.md ممنوع کرده.

⚠️ Reverse Proxy روی `/api` هدر CSP را با `?` می‌گذارد — یعنی «فقط اگر
پاسخ خودش نگذاشته باشد». صفحه چاپ برچسب CSP سخت‌گیرتر خودش را دارد و
نباید بازنویسی شود.

## Stack

Node 22 + Fastify + **Kysely** · React + Vite · PostgreSQL 16 · pnpm ·
Docker Compose تک‌سرور. **بدون Redis، بدون Kubernetes، بدون Microservice.**

TypeScript بدون مرحله Build اجرا می‌شود (`node --experimental-strip-types`).
یعنی کد باید در زیرمجموعه **strip-only** بماند: بدون `enum`، بدون
`namespace`، و **بدون parameter property** در سازنده کلاس. اگر `tsc`
سبز بود ولی Node خطای `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` داد، همین است.

Kysely نه Prisma: عملیات مالی به SQL صریح نیاز دارد (`FOR UPDATE`، CTE،
توابع تجمیعی). `sql.raw` با ورودی کاربر **ممنوع مطلق**.

## چه چیزی نساز

تا وقتی صریحاً خواسته نشده، این‌ها ساخته نمی‌شوند — دلایل در بخش M بازبینی:

Microservice · Redis · **حالت آفلاین کامل** · **کد ارتباط با دستگاه
کارت‌خوان** (تا دریافت مستندات کتبی SDK از PSP) · اتصال مستقیم به
سامانه مالیاتی · موتور Import عمومی · **AI پیشنهاد سایز** · UI چند
شعبه · RFM و پیش‌بینی تقاضا

دوتای پررنگِ باقی‌مانده ظرفشان ساخته شده ولی خودشان نه — و تفاوت مهم
است:

- **صف آفلاین** ≠ حالت آفلاین کامل — و از FND-002 به بعد **وصل است**،
  نه یک کلاسِ بی‌مصرف‌کننده. سه تکه دارد:
  `offline-queue.ts` (منطق)، `queue-store.ts` (ذخیرهٔ **پایدار** روی
  `localStorage`)، و `sale-queue.ts` (تنها مصرف‌کننده).
  درخواستی را که سرور **قبولش می‌کرد اگر شبکه قطع نمی‌شد** نگه می‌دارد
  و با **همان** `Idempotency-Key` دوباره می‌فرستد. هیچ تصمیم مالی در
  مرورگر گرفته نمی‌شود: نه شماره‌گذاری سند، نه بررسی موجودی، نه
  قیمت‌گذاری. درخواست **بدون** کلید Idempotency صف نمی‌شود و صریح رد
  می‌گیرد — ارسال دوباره‌اش فاکتور دوم می‌ساخت.
  ⚠️ **فقط `finalize` صف می‌شود، و یک تست همین را می‌شمارد.** سطرها و
  پرداخت‌ها همه آنلاین ثبت شده‌اند، پس فاکتور یک پیش‌نویس کامل **روی
  سرور** است. صف‌کردن اسکن یا پرداخت یعنی صفحه قیمت یا مانده‌ای نشان
  بدهد که سرور تأییدش نکرده — همان «حالت آفلاین کامل» که ممنوع است.
  ⚠️ **`memoryStore()` تنها ذخیره بود و کامنت خودش می‌گفت با بسته‌شدن
  صفحه از بین می‌رود.** یعنی وعدهٔ «فروش را نگه می‌دارم» با یک Reload
  باطل می‌شد. `localQueueStore()` همگام می‌نویسد، پس تا وقتی به
  صندوق‌دار می‌گوییم «در صف است» واقعاً نشسته.
  ⚠️ **دادهٔ خراب صف را پاک نمی‌کند و سهم پرِ ذخیره بلعیده نمی‌شود.**
  هر دو راه‌حلِ «تا صفحه بالا بیاید» هستند و هر دو یک فروش را بی‌صدا
  می‌خورند: «۰ فروش در انتظار» برای صفی که خوانده نشده، از هر خطایی
  خطرناک‌تر است چون آرام‌بخش است.
- **رجیستری درایور** ≠ درایور. `platform.device_driver` جایی است که
  مستندات SDK بنشیند؛ `is_implemented` پیش‌فرض `false` است و
  `treasury.set_device_driver()` وصل‌کردن پایانه به درایورِ
  پیاده‌نشده را **رد می‌کند**. بدون آن، مالک کارت‌خوانی را انتخاب
  می‌کرد و اولین پرداخت واقعی معلق می‌ماند.
  دستگاه تازه و مستندات SDK از **صفحه تنظیمات** اضافه و عوض می‌شوند
  (`platform.upsert_device_driver()`) — کارت‌خوان‌ها عوض می‌شوند و
  زیادتر می‌شوند، و هیچ‌کدام نباید مهاجرت بخواهد.
  ⚠️ **`is_implemented` از آن مسیر روشن نمی‌شود.** آن یک واقعیت
  درباره کدِ این مخزن است، نه یک تنظیم؛ روشن‌شدنش از UI یعنی دور
  زدنِ همان نگهبان بالا. فقط مهاجرتی که هم‌زمان Handler را می‌آورد.
  ⚠️ راز در `driver_config` — و در `notes` و `sdk_doc_url` —
  نمی‌نشیند: مقدارشان در `audit_log` می‌رود و صفحه تنظیمات نشانشان
  می‌دهد. `upsert_device_driver` الگوی «api_key: …» را رد می‌کند.
- **درایورِ در استفاده بازنشسته نمی‌شود.**
  `treasury.set_device_driver()` فقط **در لحظه اتصال** فعال‌بودن را
  می‌سنجد؛ بازنشستگی بی‌صدا یعنی پایانه‌ای که به درایوری اشاره
  می‌کند که دیگر در هیچ فهرستی نیست. اول پایانه جدا می‌شود.
- **پیشنهاد سایز** ≠ AI. `catalog.fit_score()` حساب فاصله است:
  اختلاف اندازه تقسیم بر تحملِ همان کلید. قابل توضیح و قابل بازرسی.
  مدل واقعی به داده فروش و مرجوعیِ همین فروشگاه نیاز دارد که هنوز
  وجود ندارد.
- **پیام‌رسان** یک Webhook عمومی است، نه ادغام با واتساپ یا تلگرام:
  هر کدام مجوز و قواعد تجاری خودشان را دارند و انتخابش تصمیم مالک
  است. `notify.webhook_url` باید `https` باشد — لینک فاکتور توکن
  دارد.

## پیش از ورود داده واقعی

سیستم آماده بهره‌برداری **نیست** تا این‌ها تصویب شوند:
کدینگ حساب و روش بهای تمام‌شده (حسابدار) · وضعیت مشمولیت مالیاتی
(مشاور مالیاتی) · دوره تسویه و کارمزد کارت‌خوان (PSP).

`SELECT key, value, description FROM platform.setting WHERE requires_approval;`

## اسناد

- @docs/ADR-001-stack.md — چرا این Stack، و چرا منطق مالی در دیتابیس
- @docs/ADR-002-design-language.md — Liquid Glass ناحیه‌بندی‌شده + پالت قفل‌شده
- `docs/ADR-003-posting-batch.md` — چرا دوره ثبت از شیفت صندوق جداست
- `docs/ADR-004-cheques.md` — چرا چک یک سند مستقل است، نه یک روش پرداخت
- `docs/ADR-005-device-trust.md` — اعتماد دستگاه، ارتقای نشست و CSRF
- `docs/ADR-006-costing.md` — آخرین قیمت خرید، تجدید ارزیابی، و اینکه
  سود کجا شناسایی می‌شود
- `docs/ADR-007-realtime-sync.md` — Push موجودی و قیمت به سایت:
  **ساخته شد** (مهاجرت ۰۵۷)، پیش‌فرض **خاموش**.
  ⚠️ مهم‌ترین قیدش: پیام **مقدار مطلق** می‌برد نه تفاضل، چون تحویل
  Outbox «حداقل یک بار» است نه «دقیقاً یک بار». و جهت معکوس (سایت →
  Core برای موجودی) عمداً ساخته نمی‌شود: حلقهٔ Ping-pong.
  ⚠️ **گیرندهٔ تست یکپارچه ووکامرس نیست** — یک سرور محلی با همان
  قرارداد. پس تأخیر واقعی، چرخهٔ عمر افزونه و HPOS **هنوز سنجیده
  نشده‌اند** و در فهرست پیش از بهره‌برداری مانده‌اند.
- @docs/SECURITY.md — Argon2id، نشست مات، WebAuthn، زنجیره تأمین
- `docs/DEPLOYMENT.md` — استقرار روی سرور، TLS، CSP، بکاپ، عیب‌یابی
- `docs/DATA-MIGRATION.md` — انتقال کالا، مشتری، موجودی و افتتاحیه
- `docs/ACCOUNT-MAPPING-HOLOO-DASHT.md` — برگهٔ تطبیق کدینگ برای میز
  حسابدار. ⚠️ ستون «کد هلو/دشت» عمداً **خالی** است: هیچ نصب زنده‌ای در
  دسترس نبود و نوشتنش از حافظه یعنی تأیید چیزی که ندیده‌ایم.
- `integrations/woocommerce/README.md` — نصب افزونه، واحد پول، عیب‌یابی
- `docs/design-proof.html` — نمونه بصری داشبورد و صندوق

بازبینی کامل پروژه:
https://claude.ai/code/artifact/8fa9f0f7-7a11-4cc0-8f21-c3ff51c46093
