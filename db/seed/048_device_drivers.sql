-- =====================================================================
-- درایورهای دستگاه — داده مرجع
-- =====================================================================
-- ⚠️ **همه با `is_implemented = false`.**
--
-- ثبت یک درایور اینجا یعنی «می‌دانیم وجود دارد و جایش را ساخته‌ایم»،
-- **نه** «کار می‌کند». تا مستندات کتبی SDK از PSP نرسد، هیچ Handlerی
-- نوشته نمی‌شود — بند «چه چیزی نساز» در CLAUDE.md.
--
-- بدون این تفکیک، مالک یک کارت‌خوان را انتخاب می‌کرد، پایانه را به آن
-- وصل می‌کرد، و اولین پرداخت واقعی در سکوت شکست می‌خورد — یا بدتر،
-- معلق می‌ماند و پول مشتری بلاتکلیف.
--
-- وقتی SDK رسید: `sdk_doc_url` را پر کنید، Handler را بنویسید، و
-- `is_implemented` را `true` کنید. هر سه از راه تنظیمات و بدون
-- مهاجرت.
-- =====================================================================

INSERT INTO platform.device_driver
  (code, label, device_kind, vendor, is_implemented, sort_order)
VALUES
  -- کارت‌خوان‌های رایج ایران. فهرست باز است؛ افزودن یکی تازه یک
  -- `INSERT` است، نه یک مهاجرت.
  ('sep',        'سامان کیش (SEP)',      'card_terminal', 'سامان کیش',      false, 10),
  ('behpardakht','به‌پرداخت ملت',        'card_terminal', 'به‌پرداخت ملت',  false, 20),
  ('asanpardakht','آسان پرداخت',         'card_terminal', 'آسان پرداخت',    false, 30),
  ('pasargad',   'پاسارگاد (PEP)',       'card_terminal', 'پرداخت الکترونیک پاسارگاد', false, 40),
  ('parsian',    'پارسیان',              'card_terminal', 'تجارت الکترونیک پارسیان',   false, 50),
  ('sadad',      'سداد (ملی)',           'card_terminal', 'سداد',           false, 60),
  ('irankish',   'ایران کیش',            'card_terminal', 'ایران کیش',      false, 70),
  -- چاپگر رسید. جایش هست ولی کدش نیست — پروژه امروز لینک می‌فرستد،
  -- نه رسید چاپی.
  ('escpos',     'چاپگر حرارتی ESC/POS', 'printer',       'عمومی',          false, 110)
ON CONFLICT (code) DO NOTHING;
